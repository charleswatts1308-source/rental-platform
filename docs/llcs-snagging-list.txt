LLCS SNAGGING LIST
==================

Minor bugs and rough edges spotted while testing the Landlord Contact Service
UI. These are deferred on purpose - none are blockers. They'll be fixed in a
batch once the whole LLCS lifecycle has been walked through, so testing isn't
interrupted.

If something actually BLOCKS testing (a crash, a 500, data loss, a broken
flow), don't park it here - flag it straight away.

To add a snag: copy the dashed block below, fill it in, leave Status as "open".
Severity guide: cosmetic = looks off / minor = awkward / functional = wrong.


#
Page:
Severity:
Saw:
Expected:
Status:
Notes:

--------------------------------------------------------------------
#1
Page:        Main nav dropdown
Severity:    cosmetic
Saw:         Dropdown still titled "Your Rental Profile" after rentals removal
Expected:    A title that fits its contents (Properties + Repair Cases only)
Status:      open — scheduled: Phase 3 ride-along (per design update §6)
Notes:       Flagged during rentals removal (commit 929d2f6). Maybe "Your Tenancy".
--------------------------------------------------------------------
#2
Page: Cases - landlord agent email not shown
Severity:
Saw:
Expected:
Status:      open
Notes:
--------------------------------------------------------------------
#3
Page:        Email - stage 1 initial notice (all stages, any demo case)
Severity:    minor
Saw:         "My description of the issue:" followed by boilerplate
             "Please arrange to inspect and repair the reported issue."
Expected:    A plausible per-case description (UI-created cases get the
             tenant's typed statement; demo cases need realistic text).
Status:      closed — superseded by D9 (cases.description fixed at
             creation, immutable). Fix lands in Phase 3 implementation.
Notes:       Cause: dev:letter falls back to its default --statement and
             dev:lifecycle calls it without one.
Fix:         D9 introduces cases.description on the cases table; the
             dev:lifecycle SPECS row gains a description column and the
             dev:letter --statement filler default dies. Implementation
             in Phase 3.
--------------------------------------------------------------------


ALREADY FIXED THIS PASS (for reference)
- Plain-text landlord replies showed as an empty card on the case page.
  Fixed in commit 13d01b1.

#4
Page:        Case reference (system-wide: dashboard, notification emails, URLs)
Severity:    Low — cosmetic/UX; not blocking. Pre-flip change, zero migration cost.
Saw:         Case reference is a 12-char mixed-case random string (e.g. Bmjomam2aaSV).
Expected:    Short, human-quotable reference: 6 uppercase chars, human-safe alphabet,
             case-insensitive, random, unique, never reused.
Status:      Open — design decided, ready for CC.
Notes:       Use case is a tenant quoting the reference to a landlord verbally/in
             writing ("re: case TRE3YU"), so it must survive being said aloud and
             retyped. Security is NOT carried by the reference — per-tenant login +
             server-side ownership checks already gate case access, so the reference
             can be short without weakening anything. Random (not sequential) so it
             leaks neither case volume nor allows walking. Only preprod seed data
             exists, so this is a free change now; would be costly post-go-live.


# Snag list entry

```
#            <next-number>
Page:        Tenant case dashboard / case correspondence flow
Severity:    Medium — capability gap, not a defect. Design decision required before
             implementation. Not blocking current testing, but affects core flow.
Saw:         Conversation is effectively half-duplex. Landlord can reply freely by
             email (round-trip proven working). Tenant receives a notification and
             can VIEW the reply in the dashboard, but has no action to RESPOND /
             supply further information back to the landlord.
Expected:    Tenant should have a "reply / add information" action at appropriate
             case states, sending a new message to the landlord (with the same
             {token}@mg.renters.rent Reply-To so the landlord's reply round-trips),
             logged to case_messages, and transitioning the case state accordingly.
Status:      closed — superseded by D8 / Phase 3 (design update §6). All open
             design questions below resolved by D8's per-state availability
             table and the transition-to-awaiting_landlord rule.
Notes:       Surfaced when landlord replied "you have omitted the details of the
             issue" and there was no tenant path to respond with the detail.
             This is a STATE-MACHINE change (new transition(s)), not just a button:
             must decide, per state, whether tenant-reply is valid and what it
             transitions to. Open design questions:
               - Which states allow tenant reply? (awaiting_tenant_review = yes;
                 awaiting_landlord = maybe/add-info; on_hold/resolved/dormant = ?)
               - What does tenant reply transition to? (likely awaiting_tenant_review
                 -> awaiting_landlord — ball back to landlord)
               - Reuses outbound-letter + token machinery? (each tenant reply is
                 effectively another letter in the thread)
               - Token continuity over a long thread vs 90-day expiry model.
             SEPARATE but related: the trigger for this was the BLANK DESCRIPTION
             defect (still un-diagnosed: seed-data gap vs CaseNotice template bug).
             That has its own check pending and must not be masked by this feature.
```

--------------------------------------------------------------------
#5
Page:        Login page (when arriving from a notification email)
Severity:    minor
Saw:         Click "Open this case" in landlord-reply notification → login
             page loads with empty email field, requires both email and
             password to proceed.
Expected:    Email pre-populated with the tenant's address (the notification
             went to that address; pre-populating doesn't leak anything).
Status:      closed — superseded by D12 (magic-link sign-in on all Phase 3-
             touched emails makes the pre-fill pointless: clicking the
             link signs the tenant in directly).
Notes:       Cause: LandlordReplyReceived sends a plain unsigned
             route('cases.show', ...) URL; the auth redirect to /login has
             no identity hint, and login.blade reads only old('email').
Fix:         D12 magic-link supersedes — pre-fill no longer needed.
             Implementation in Phase 3.
--------------------------------------------------------------------
#6
Page:        Login page (one-click sign-in from notification email)
Severity:    minor — UX win; security trade-off named
Saw:         Tenant must type their password every time they arrive from
             "Open this case" — even though they just clicked a link sent
             to their own inbox.
Expected:    One-click sign-in: clicking the notification logs the tenant
             in for that session and lands them directly on the case page.
Status:      closed by D12 (magic-link sign-in on all Phase 3-touched
             emails — threat-model argument adopted verbatim).
Notes:       Threat model: anyone with inbox access can sign in — same bar
             as the existing password-reset flow (inbox access already =
             account takeover), so no new security ceiling is lowered.
             UX win is large if reply-and-act becomes a frequent flow.
Fix:         D12 mechanics: signed single-use short-expiry token table,
             route + middleware, Auth::login on click. Implementation
             in Phase 3.
--------------------------------------------------------------------
#7
Page:        Create-case form — landlord email + name fields
Severity:    minor
Saw:         When the typed landlord_email already exists in
             landlord_contacts, the name field stays empty. Tenant can type
             a different name with no warning — controller silently keeps
             the existing row's name (correct backend behaviour, no UX cue).
Expected:    On a recognised email, name field auto-fills with the existing
             landlord's name and shows a "this landlord is already in our
             system" hint (and probably becomes read-only).
Status:      open
Notes:       Form is plain static HTML (resources/views/cases/create.blade.php):
             no JS, no lookup endpoint. Controller find-or-create at
             CaseController.php:262-268 silently discards form name on a hit.
Fix:         Add GET /api/landlord-lookup?email=... (auth-only, rate-limited),
             plus ~30 lines of JS on the create form: on email blur, fetch
             and pre-populate the name field with a "known landlord" hint.
--------------------------------------------------------------------

--------------------------------------------------------------------
#8
Page:        System-wide — outbound mail delivery status
Severity:    Medium — evidential blind spot. Capability gap, not a defect.
             Post-cutover feature, not part of the silence-model rewrite.
Saw:         The platform records a letter as sent (frozen in case_messages,
             accepted by Mailgun) but has no knowledge of whether it was
             delivered. Evidenced 2026-06-06 on gafol: 10 sends accepted,
             4 failed at Gmail with 550 spam blocks — the system showed no
             trace; only a human reading the Mailgun dashboard found out.
             (Sandbox-domain reputation caused the failures — production's
             DMARC-aligned mg.renters.rent won't fail at that rate — but
             the blind spot exists regardless of rate.)
Expected:    Platform consumes Mailgun delivery webhooks (delivered /
             failed / bounced events), stamps delivery status on the
             case_messages row, and surfaces failures to the tenant
             ("your letter could not be delivered to <address>") with a
             path to act (correct the address, resend).
Status:      CLOSED 2026-08-01 — superseded by #25, which describes the
             same blind spot and carries the actual build plan (envelope
             metadata correlation, the columns, the webhook controller,
             the escalation-counter invariant). Nothing here is fixed;
             this entry is retired as a DUPLICATE, not as done.
             The two live-fire observations below are the field evidence
             for #25 and have been carried across to it. Kept here for
             provenance — work #25, not this.
Notes:       For a service whose promise is "your landlord was notified,
             here's the proof", sent-but-not-delivered is the one
             discrepancy that must never be silent. 2026-06-06 test data:
             40% silent loss on a reputation-damaged domain.
             2026-06-07 (Phase 3 live-fire): repeat — Gmail returned
             5.7.1 and blocked 4 sandbox sends; the Mailgun log was the
             only ground truth, the platform showed nothing. Second
             independent confirmation of the same blind spot; strengthens
             the case for consuming delivery/failure webhooks before
             go-live.
--------------------------------------------------------------------
#9
Page:        dev:reset truncation list (DevReset.php) — silence-model
             tooling gap surfaced during silence-phase-2b live-fire.
Severity:    minor — corrupts shadow-report storytelling, no data loss.
Saw:         silence_shadow_log is not in DevReset::TABLES, so
             dev:lifecycle's reset preserves yesterday's shadow rows.
             Today's reseed reuses case IDs 1..8; silence:shadow-report
             aliases stale case_id=N rows to today's freshly-minted
             slug for N, producing a confusing report that looks like
             today's sweep walked multiple dates.
Expected:    dev:reset wipes silence_shadow_log alongside the other
             transactional tables; a fresh dev:lifecycle yields a
             shadow report that contains only today's rows.
Status:      open — scheduled: Phase 3 ride-along (per design update §6)
Notes:       Confirmed 2026-06-07 gafol live-fire: yesterday's three
             pretend probes (06-21, 07-01, 07-11) survived overnight,
             merged into today's 07-11 swept_at group, and inflated
             COUNT to 12 against an actual 6 cases swept once.
Fix:         Add 'silence_shadow_log' to DevReset::TABLES (constant
             at top of the class). One-liner, no migration.
--------------------------------------------------------------------
#10
Page:        silence:sweep summary line (SilenceSweep.php:117-137).
Severity:    cosmetic — count display only; underlying rows correct.
Saw:         Re-tally filters `swept_at = $now AND is_pretend = $isPretend`.
             For pretend mode, $now is the deterministic
             `pretend_today 09:00:00`, so two pretend sweeps on the
             same date share a swept_at. The second sweep's summary
             counts BOTH runs' rows (e.g. 12 verdicts reported for 6
             cases × 2 invocations).
Expected:    Summary counts only rows written by the current
             invocation.
Status:      open — scheduled: Phase 3 ride-along (per design update §6)
Notes:       Cosmetic — every individual shadow row is correct and
             carries its own swept_at + pretend_today + verdict.
             Only the operator-facing summary tally inflates.
Fix:         Track the row IDs written this invocation (collect from
             writeShadowRow / writeExceptionShadowRow returns) and
             tally from those, instead of a swept_at re-query. Or
             switch swept_at to the real wall-clock even on pretend
             runs and keep pretend_today as the only horizon column.
--------------------------------------------------------------------
#11
Page:        Stage 2+ escalation letters (click path + sweep path).
             Render path issue, not seed data.
Severity:    minor — pre-existing UX/evidential, not a 2b regression.
Saw:         Letters rendered "My description of the issue:" followed
             by an EMPTY value on every stage past the first send.
             Confirmed on gafol stage-2 auto-escalation 2026-06-07.
Expected:    A meaningful issue description on every escalation
             letter (the landlord-facing record is supposed to remind
             them what the case is about, every time).
Status:      closed — superseded by D9 (cases.description fixed at
             creation, immutable, on every outbound). Fix lands in
             Phase 3 implementation.
Notes:       Root cause: SendCaseNotice::execute's $tenantStatement
             defaults to null, and both subsequent callers pass null:
             - CaseController::sendNext (line 87) — tenant click.
             - SilenceSweep::handleCase (line 200) — auto-escalation.
             Only first-send (create-case form line 245-250) passes
             the tenant's typed description. case_messages.tenant_
             statement is per-message; cases table has no description
             column. So there is no current source for stage 2+.
             Design doc lists {{issue_description}} as a whitelist
             placeholder but does not specify a source for sweep /
             click escalation rounds.
Fix:         D9 picks option 2 (persist on case): add cases.description,
             populate at case creation, pull on every send across both
             click and sweep paths. Tenant-reply path (D8) also uses
             the same source. Implementation in Phase 3.
--------------------------------------------------------------------
#12
Page:        dev:lifecycle SPECS table (DevLifecycle::SPECS) — seed data.
Severity:    cosmetic — demo-data realism only.
Saw:         Per-case description text doesn't match the case's repair
             category (e.g. a "damp/mould" category row carries a
             description about a different issue). Spotted during the
             2026-06-07 gafol live-fire after D9 populated descriptions.
Expected:    Each SPECS row's 5th (description) column reads as a
             plausible statement for that row's category_key.
Status:      open — cosmetic, seed-data only.
Fix:         Realign the description column against category_key in the
             SPECS array. No code/migration — array text only.
Notes:       Re-observed 2026-06-13 D15 gafol live-fire (dashboard
             "Heating and hot water" vs a damp/bathroom D9 header block).
             Same root cause; not a D15 issue. Logged here, not duplicated.
--------------------------------------------------------------------
#13
Page:        dev:age-clock gauge / operator output.
Severity:    cosmetic — operator-facing display, sweep logic unaffected.
Saw:         For tenant-ball cases the gauge prints the escalation
             interval (landlord-side threshold) rather than the
             nudge/dormancy ladder thresholds that actually govern a
             tenant-side clock.
Expected:    Gauge reflects the threshold set that applies to the
             case's current ball (nudge.first_days / nudge.second_days /
             dormancy_days when ball=tenant).
Status:      open — cosmetic.
Notes:       Confirmed 2026-06-07 gafol live-fire. The sweep verdicts
             were correct throughout; only the human-readable gauge
             mislabels the horizon for tenant-ball cases.
Fix:         Branch the gauge readout on ball_with and pull the matching
             threshold set.
--------------------------------------------------------------------
#14
Page:        Case page (cases.show) — after a hold is released by sweep.
Severity:    cosmetic — stale display; state machine correct.
Saw:         After the sweep releases an expired hold (OnHold ->
             AwaitingLandlord), the old hold_until value is still shown
             on the case page and is not nulled on release.
Expected:    Once a hold is released, hold_until either reads as past/
             cleared or is not surfaced as an active pause.
Status:      open — cosmetic.
Notes:       The design keeps hold_until as a historical record on the
             row (so don't necessarily null the column) — the snag is
             the case page presenting a released hold as if still live.
             Confirmed 2026-06-07 gafol live-fire (step 15).
Fix:         Gate the hold_until display on status === on_hold (see #15;
             same root predicate).
--------------------------------------------------------------------
#15
Page:        Case page (cases.show) — "Next escalation" line.
Severity:    cosmetic — informational line shown in the wrong state.
Saw:         The "Next escalation" projection is rendered while the case
             is on_hold, where escalation is paused and the line is
             meaningless.
Expected:    "Next escalation" suppressed (or replaced with the hold
             status) while status === on_hold.
Status:      open — cosmetic.
Notes:       Confirmed 2026-06-07 gafol live-fire. Same state-awareness
             gap as #14.
Fix:         Suppress the escalation projection on on_hold (and other
             non-escalating states).
--------------------------------------------------------------------
#16
Page:        Case page / letter context — "Stage 1 of 4" progress label.
Severity:    cosmetic — hardcoded denominator.
Saw:         The stage label hardcodes 4 as the total notice count
             instead of reading escalation.max_notices from settings.
Expected:    Denominator reads the live escalation.max_notices value so
             the label tracks the configured ladder length.
Status:      open — cosmetic.
Notes:       Confirmed 2026-06-07 gafol live-fire. Harmless while
             max_notices=4 but drifts if the setting changes.
Fix:         Replace the literal 4 with Setting::get('escalation.max_notices').
--------------------------------------------------------------------
#17
Page:        dev:reset truncation list (DevReset.php) — deploy tooling.
Severity:    minor — crashes mid-deploy on a partially-migrated DB.
Saw:         During the 2026-06-07 gafol Phase 3 deploy, dev:reset
             (run BEFORE migrate per the Phase 3 sequence) crashed when
             its TABLES list named a table that the pending migrations
             had not created yet.
Expected:    dev:reset skips tables that don't exist instead of erroring,
             so it is safe to run on a not-yet-fully-migrated schema
             (which is exactly the Phase 3 dev:reset-before-migrate order).
Status:      open — minor; worked around on the day by ordering.
Notes:       Surfaced precisely because Phase 3 inverts the usual order
             (reset then migrate). The reset must tolerate a schema that
             is missing tables it would otherwise truncate.
Fix:         Guard each truncation with Schema::hasTable($name) before
             truncating; skip-and-continue on a miss.
--------------------------------------------------------------------
#18
Page:        Migrations — implicit ON UPDATE CURRENT_TIMESTAMP trap on
             NOT NULL TIMESTAMP columns sitting FIRST in their table.
Severity:    minor — latent landmine; dormant on insert-only tables.
Saw:         Audit (2026-06-07, prompted by the magic_login_tokens Q1
             finding) of every CREATE TABLE migration. On MariaDB with
             explicit_defaults_for_timestamp=OFF (gafol/prod default),
             the FIRST timestamp column in a table, if NOT NULL with no
             explicit default, is silently given DEFAULT CURRENT_TIMESTAMP
             ON UPDATE CURRENT_TIMESTAMP — so any UPDATE to the row
             rewrites that column to now() (and the value lands in the DB
             session TZ, hence the "+1h" BST gap seen on magic links).
             Four tables carry the same first-column trap, all currently
             insert-only so the trap is unexercised:
               - page_views.view_date_time
               - error_logs.error_date
               - file_attachments.uploaded_date  (drop_rentals migration;
                 may not be migrated on prod yet)
               - silence_shadow_log.swept_at      (evidential log — would
                 corrupt swept_at if a shadow row is ever updated)
             Safe (verified, no action): reply_tokens (issued_at is
             useCurrent → explicit default, expires_at is nullable and
             not first), cases/case_events/case_messages/contact_messages
             (first timestamp is nullable or useCurrent), and every
             ->timestamps()/nullable() column.
Expected:    No table relies on the implicit ON UPDATE; first timestamp
             columns are either DATETIME or carry an explicit default.
Status:      open — fixed for magic_login_tokens only (Q1, this commit:
             expires_at → dateTime). The four above left as-is; flagged
             so a future UPDATE path on any of them doesn't silently
             corrupt the column.
Notes:       Invisible to the test suite — SQLite has no ON UPDATE
             CURRENT_TIMESTAMP semantics, so these only bite on MariaDB.
Fix:         Per table, switch the offending column to dateTime() (never
             receives the implicit clause) or give it an explicit default;
             since these are pre-flip, a migrate:fresh re-applies the
             amended migration on gafol (same path as the Q1 fix).
--------------------------------------------------------------------
#19
Page:        Tenant reply UI (cases.reply) — outbound tenant message.
Severity:    feature request — NOT a defect. Deferred, phase TBD.
Saw:         The tenant reply form (D8, Phase 3) sends text only. During
             the 2026-06-07 gafol live-fire, a tenant replying about a
             worsening issue naturally wanted to attach a photo of the
             deterioration and had no way to.
Expected:    Tenant replies should accept attachments (same JPG/PNG/PDF
             ≤2MB, ≤6 files rule as create-case), promoted onto the
             outbound tenant case_messages row and dispatched on the
             CaseNotice to the landlord — mirroring the create-case
             attachment path that already exists.
Status:      open — deferred feature, phase TBD. Explicitly OUT OF SCOPE
             for Phase 3 (the brief lists "attachments on tenant replies"
             under untouched).
Notes:       Surfaced by real usage in the live-fire, not by a test gap.
             The machinery already exists end to end for create-case
             (stagePreviewPhotos → promotePreviewPhotos → MessageAttachment
             → CaseNotice::attachments). A tenant-reply version reuses the
             same SendCaseNotice attachmentInputs parameter, which the
             reply path currently calls without attachments.
Fix:         Add a file input to the reply form + photo validation/staging
             in CaseController::reply, thread attachmentInputs into the
             tenant-reply SendCaseNotice call. Reuses existing helpers.
--------------------------------------------------------------------
#20
Page:        D9 description header block — dark mode (authorise-preview
             cases.authorise, and likely any dark-mode letter/mail view).
Severity:    cosmetic — content correct, contrast only.
Saw:         The D9 header block (property / case reference / issue) renders
             near-invisible in dark mode: low contrast against the dark
             background, only legible when the text is selected. Spotted on
             the D15 authorise-preview during the 2026-06-13 gafol live-fire.
Expected:    The header block stays legible in dark mode (explicit text
             colour, or a background that holds contrast in both themes).
Status:      open — cosmetic. Surfaced by D15 but the block predates it
             (D9, Phase 3); affects every view that renders the block.
Notes:       The block uses a fixed light-grey background with no explicit
             text colour (LetterTemplateRenderer::buildHeaderBlock), so dark
             mode inherits a light-on-light combination.
Fix:         Set an explicit text colour on the header block (and/or a
             background that survives both themes) in buildHeaderBlock.
             Renderer change — confirm it doesn't disturb the frozen
             evidential letter bodies (header is wrapper, not body).
--------------------------------------------------------------------
#21 — exhausted-case "abandon" collision (UI confusion)
Exhausted page shows two controls that both say "abandon": the cosmetic
stance dropdown ("Abandoned" value, label-only, writes exhausted_stance)
and the real "Abandon this case" terminal action (transitions to
abandoned, stamps closed_at). Tenant can't tell them apart. Machine is
correct — both work as built; they just collide on the word.

RULING (reverses D14 cosmetic-only): converge on CC's model —
- drop "Abandoned" from the cosmetic stance dropdown
- keep the real "Abandon this case" as the genuine terminal close
- keep "Unresolved" as cosmetic, stays escalation_exhausted, revivable
- protect: revival via the Unresolved path must survive (a reply always
  revives the not-given-up tenant)
Update D14 in the design doc to match (cosmetic stance = unresolved only;
abandoned is a real terminal close per D5).

Also note: "Next escalation" date reappears on the abandoned card
(absent correctly on exhausted) — minor, fold into the same pass.
#21
Page:        Exhausted case page action panel (cases._action_panel,
             status=escalation_exhausted). Surfaced on the 2026-06-14 gafol
             D14 live-fire.
Severity:    UX confusion — machine behaviour is CORRECT; no data harm.
Saw:         The exhausted page exposes TWO controls that both say "abandon"
             with different meanings: (1) the generic terminal "Abandon this
             case" action (transitions to status=abandoned, stamps closed_at;
             deliberate, per design doc D5 "tenant can still close"), and
             (2) the cosmetic exhausted_stance selector whose values include
             "Abandoned" (label only — leaves the case in escalation_exhausted,
             no state change). A tenant picked (1) intending (2): the case
             closed when they meant only to label it.
Expected:    A tenant framing the outcome as "abandoned" should not be able
             to confuse that with truly closing the case.
Status:      open — carries a DESIGN QUESTION for Charlie. Two coherent fixes:
             (a) keep stance cosmetic as ruled, but disambiguate the labels/
                 placement so the two "abandon"s cannot be confused; or
             (b) adopt the "Abandon truly closes the case; Unresolved = stay
                 exhausted (revivable)" model — cleaner UX, but REVERSES the
                 D14 cosmetic ruling and loses the abandoned-but-revivable
                 state (a later landlord reply would not revive an abandoned
                 case; recurrence = new case, the standard D8 terminal rule).
Notes:       exhausted_stance IS wired (CaseController::setStance, POST
             cases.stance, policy setStance; covered by a passing test). The
             collision is the issue, not a missing build. Both controls are
             individually to-spec. My read (CC): (b) is the better UX but is
             Charlie's call since it reverses the cosmetic ruling; I'd protect
             revival via the Unresolved path either way.
Fix:         Per Charlie's ruling — either disambiguate (a) or rebuild to (b)
             (drop "Abandoned" from the stance enum/selector, keep the real
             abandon action, reword the stance prompt as a private note).
--------------------------------------------------------------------
#22
Page:        Admin UI — repair_categories has no editor (Surfaces A/B exist,
             categories don't). Surfaced 2026-06-27 during dotrent
             production-candidate build.
Severity:    minor — capability gap, NOT a defect. Non-blocking.
Saw:         The D16 admin layer ships Surface A (letter templates) and
             Surface B (settings), but repair_categories has no admin screen:
             add / rename / reorder / retire is phpMyAdmin-only.
Expected:    A "Surface D"-style read/write admin editor for repair categories
             (label, sort_order, active, requires_description), mirroring the
             Surface A/B pattern.
Status:      open — deferred, not in D16 scope. Non-blocking: categories are
             static reference data, seeded (11 rows via RepairCategorySeeder),
             and the create-case dropdown reads them correctly.
Notes:       Candidate for a future admin addition alongside the deferred
             letter_templates.active toggle (NEXT-SESSION "New deferred items").
             Both are admin-surface gaps over load-bearing reference/config
             data that currently live in phpMyAdmin only.
Fix:         Add a categories admin surface (CRUD over repair_categories,
             active-toggle + sort_order reorder), following the Surface A/B
             controller + view pattern. Future phase, TBD.
--------------------------------------------------------------------
#23
Page:        SECURITY — Mailgun credentials exposed in a transcript
             (4 Jul 2026). Not a UI snag; parked here as a pre-live to-do.
Severity:    functional — SECURITY. Do before serious live running.
Saw:         `config:show services.mailgun` printed MAILGUN_SECRET and
             MAILGUN_WEBHOOK_SIGNING_KEY in full into a chat transcript on
             4 Jul 2026. These are LIVE production credentials. Still not
             rotated as of 13 Jul (9 days).
Expected:    Both keys rotated; no live credential sitting in a transcript.
Status:      CLOSED 2026-08-01 — both keys rotated. The exposed credentials
             are dead; the transcript from 4 Jul no longer carries anything
             live. Open for 28 days.
             Dotrent: N/A — RETIRED as of 1 Aug 2026, so the "rotate on
             both boxes" half of the original Fix line no longer applies.
             renters.rent is the only box sharing mg.renters.rent.
Notes:       Matters MORE now than it did on 4 Jul, not less: Mailgun inbound
             is live on renters.rent and carrying real landlord replies. The
             signing key is precisely what proves an inbound webhook genuinely
             came from Mailgun — while it's exposed, inbound authenticity is
             weakened and a forged reply cannot be ruled out on evidence.
             Affects BOTH renters.rent and dotrent (shared mg.renters.rent).
Fix:         Rotate MAILGUN_SECRET + MAILGUN_WEBHOOK_SIGNING_KEY in the
             Mailgun dashboard; update the .env on renters.rent AND dotrent;
             `php artisan config:cache`. Check for duplicate KEY= lines while
             in the .env (last one wins — the trap that broke the allowlist).
             Then send one test letter + one landlord reply to confirm both
             directions still work. ~10 minutes.
--------------------------------------------------------------------
#24
Page:        Case — no way to correct the landlord email after the first
             letter has been sent. Surfaced 2026-07-18 walking the new-user
             journey cold.
Severity:    minor — convenience. Recoverable today (abandon + re-raise).
             NOT a confidentiality issue: a repair notice is a name, an
             address and "the boiler is broken". A letter reaching the wrong
             mailbox is mildly awkward, not a breach. Mistyped email is an
             everyday hazard every service lives with.
Saw:         The landlord contact is a property of the CASE
             (cases.landlord_contact_id), not of the property. It is captured
             once on the create-case form. Before sending, the D13 preview
             step allows an edit; AFTER sending there is no edit route on a
             case at all. A mistyped landlord email is therefore permanent:
             the only recovery is to abandon the case and raise a new one,
             losing the case reference and the record.
Expected:    A way to correct the landlord email on an existing case, so a
             typo costs a correction rather than the whole case.
Status:      open — snagged 18 Jul; PROMOTED 27 Jul from convenience to a
             data-design item after being hit in real use (a live landlord
             email needed correcting). Now the presenting symptom of a
             deeper modelling gap: the landlord contact hangs off the CASE
             and the property has no landlord link at all.
             => See docs/landlord-contact-model-gap.md for the full design
             direction (property-owned, versioned contact with change
             history; retire the global email-keyed landlord_contacts;
             ~5-6 focused days; D0-first, reconcile with the silence-model
             design doc). That note SUPERSEDES the narrow "add an edit
             button" fix sketched below — the edit path falls out of the
             property-owned contact for free.
             SEE #25: the reason a typo currently goes unnoticed is the
             absence of delivery-failure detection, not the absence of an
             edit button. Fix #25 first — it makes the typo VISIBLE, at
             which point this becomes the ordinary convenience fix it
             should be. Building this one alone would let a tenant correct
             a mistake they still have no way of knowing they made.
Notes:       Second-order: LandlordContact is a GLOBAL table deduplicated by
             email (CaseController::resolveLandlordContact, no user scoping),
             so a typo also creates a junk global contact row.
             EVIDENTIALLY SAFE TO FIX — checked: every outbound row stamps
             to_address_raw at send time (SendCaseNotice.php:116) alongside
             the frozen body_raw/subject. Sent letters are therefore
             self-contained, so repointing the case's landlord_contact_id
             changes only where FUTURE letters go and does NOT rewrite the
             record of what was already sent. (Had to_address_raw not been
             populated, this would be unfixable without falsifying evidence.)
Fix:         Add a "correct landlord email" action on the case that (a)
             repoints landlord_contact_id via the existing
             resolveLandlordContact path, (b) leaves ALL existing
             case_messages rows untouched, and (c) writes a case event
             recording the correction (from → to, when, by whom) so the
             record shows the correction rather than silently changing.
             Consider also whether the escalation counter / silence clock
             should reset on correction — arguably yes, since the new
             recipient has not yet been given the chance to respond, but
             that touches the D3 no-reset ratchet and needs a design ruling
             against llcs-silence-model-design.md before building.
--------------------------------------------------------------------
#25
Page:        Mail delivery — a letter that never arrives is indistinguishable
             from a letter that was ignored. Surfaced 2026-07-18 while
             assessing #24.
Severity:    functional — HIGH. Goes to the core claim of the product.
             Strong candidate to fix before real landlords are involved.
Saw:         Only Mailgun's INBOUND route is consumed
             (routes/web.php:152 → MailgunInboundController). Mailgun's
             delivery-event webhooks — permanent_fail (hard bounce),
             temporary_fail, complained, unsubscribed — are not consumed at
             all. Nothing in the app ever learns that an outbound letter
             failed to deliver. The case_messages row is written, sent_at is
             stamped, the silence clock starts, and the ladder proceeds.
Expected:    A hard bounce marks the case as UNDELIVERED and tells the
             tenant. Silence should mean "delivered and not answered", never
             "never arrived".
Status:      open — snagged 18 Jul.
Notes:       This is the one that matters, and it is NOT really about typos.
             The silence model's entire claim is that it can say "your
             landlord was served on the 12th and has not responded in 14
             days." With no bounce handling it will say exactly that, with
             full confidence, when the letter hard-bounced and NOBODY was
             ever served. The tenant is left holding a meticulous dated
             record of having notified no one — which is precisely the
             artefact they would be relying on in a dispute.
             A typo (#24) is only the most likely CAUSE. The same silent
             failure hits a landlord who changes agent, a full mailbox, an
             expired domain, or a spam block — all of which happen to
             tenants who typed everything correctly. So this cannot be
             fixed by being careful at data entry.
             Note the asymmetry with the inbound path, which is carefully
             built (signature verification, token resolution, quarantine).
             Outbound delivery is currently assumed to succeed.

             FIELD EVIDENCE — carried from #8 when that entry was closed
             as a duplicate on 2026-08-01. This blind spot has been
             OBSERVED TWICE, not merely reasoned about:
             - 2026-06-06, gafol: 10 sends accepted by Mailgun, 4 failed
               at Gmail with 550 spam blocks. The platform showed no
               trace. A human reading the Mailgun dashboard was the only
               reason it was ever known. 40% silent loss.
             - 2026-06-07, gafol Phase 3 live-fire: repeat. Gmail
               returned 5.7.1 and blocked 4 sandbox sends; the Mailgun
               log was again the sole ground truth, the platform again
               showed nothing.
             Both were sandbox-domain reputation failures, and
             production's DMARC-aligned mg.renters.rent will not fail at
             that rate — but the BLIND SPOT is rate-independent. At any
             rate above zero the platform reports silence it cannot
             distinguish from non-delivery.
Fix:         LIMIT (state this in any tenant-facing wording) — bounce
             handling catches "went NOWHERE", not "went SOMEWHERE WRONG".
             A hard bounce is detectable; a typo landing on a real mailbox
             belonging to a stranger delivers cleanly and Mailgun correctly
             reports success. This narrows the gap, it does not close it.

             CORRELATION IS EASY — verified 18 Jul. Earlier notes on this
             snag claimed outbound message ids would have to be captured
             first (via a MessageSent listener, touching the send path).
             That was WRONG and is retracted. CaseNotice is constructed with
             the CaseMessage itself ($this->message), and Laravel's Envelope
             supports metadata which Symfony's Mailgun transport maps to
             Mailgun 'v:' custom variables (MailgunApiTransport.php:131).
             Mailgun returns those on every delivery event as
             'user-variables'. So correlation is ONE LINE in the existing
             envelope, beside the tags: line already there:
                 metadata: ['case_message_id' => $this->message->id],
             No listener, no message-id capture, no change to send logic.

             Then, roughly a day of plumbing:
             1. The envelope metadata line (above).
             2. Migration: delivery status columns on case_messages
                (status + failed_at + failure detail). MANUAL MariaDB
                SHOW CREATE TABLE check before merge — the #18 rule.
             3. Delivery-event webhook controller — mirrors the existing
                inbound controller closely; reuse verify.mailgun.signature.
             4. Route alongside the inbound one.
             5. Tests.
             Then a few more hours: notify the tenant, and surface
             "undelivered" on the case.

             ⚠ INVARIANT — the tenant notification MUST be mail-only and
             MUST NOT create a case_messages row. Outbound system rows with
             a non-null stage_at_send ARE the escalation counter; adding one
             inflates the ladder. This is the single thing here that could
             quietly break the product.

             Also: configure the webhook in the Mailgun dashboard for BOTH
             renters.rent and dotrent, and prove it live with a deliberate
             send to a dead address — it cannot be fully verified locally.

             UNBOUNDED PART — the design questions, which is why the outside
             review comes first: should a hard bounce SUSPEND the silence
             clock (counting silence against a bounced address is counting
             nothing)? Do bounced letters count toward the escalation
             counter? What happens if the tenant never acts? All touch the
             D3 no-reset ratchet and the deliberate removal of
             tenant-attention dependency. Rule against
             llcs-silence-model-design.md — which currently assumes
             delivery — before building. See
             docs/delivery-failure-design-question.md.

             ADDENDUM 2026-08-01 — confirmed still unbuilt, plus a
             SECOND reason to do it that is not about correctness.

             Confirmed today: routes/web.php:182-184 is the only Mailgun
             route, and app/Http/Controllers/Webhooks/ holds exactly one
             controller (MailgunInboundController). No delivered / failed
             / complained / unsubscribed events are received. Whether they
             are SUBSCRIBED in the Mailgun dashboard for mg.renters.rent
             is unchecked — but nothing would receive them either way.

             (a) CORRECTNESS — as above. A `failed` event means the
             message was never accepted (wrong address, mailbox full,
             domain rejecting). That is not landlord silence, but the
             clock cannot tell the difference and will run a dead case all
             the way to escalation. The tenant should be told to check the
             address instead.

             (b) EVIDENTIAL RECORD — new, and arguably the stronger
             argument. The `delivered` event carries a delivery-status
             object with the accepting server's SMTP code, response
             string, MX host, attempt number and session duration.
             Captured against the case, that turns "letter sent" into a
             contemporaneous record of an EXTERNAL event, verifiable via
             MAILGUN_WEBHOOK_SIGNING_KEY. It also makes Mailgun's 1-day
             log retention irrelevant, since we would hold the event
             ourselves. Today the only ground truth lives in a dashboard
             that forgets after a day — see #8, where a human reading that
             dashboard was the sole reason a 40% failure rate was noticed.

             Verify the actual field names against a real payload before
             building. The list above is from documentation, not observed.

             ⚠ WORDING DISCIPLINE — binding on anything surfaced to a user
             or written into a case record. The accepting server is the MX
             responsible for the RECIPIENT'S DOMAIN (Google, Microsoft,
             etc), NOT "the landlord's mail server". Name the MX host from
             the payload. And "delivered" means ACCEPTED BY THAT SERVER —
             it does NOT mean read, and nothing in the UI or the case file
             may imply that it does. Overstating this would put a claim in
             the evidential record that the data does not support.

             LIST HYGIENE: #8 describes the same blind spot from the
             pre-cutover angle and predates this entry. Consider closing
             #8 as superseded by #25 — not actioned, flagged for a call.
--------------------------------------------------------------------
#26
Page:        Test suite — view-level assertions anchored on UI copy are
             fragile, and assertDontSee ones rot SILENTLY. Surfaced
             2026-07-18 by a one-word label rename.
Severity:    minor — test-quality / false-confidence. No user impact.
             Worth a deliberate pass, not urgent.
Saw:         Renaming the case-page label "Hold until" -> "Paused until"
             touched two assertions in D16/DisplayPredicateTest:
               - assertSee('Hold until')      -> FAILED loudly. Correct.
               - assertDontSee('Hold until')  -> KEPT PASSING, vacuously.
             The second was asserting the absence of a string that no longer
             existed anywhere in the app. It would have carried on green even
             if the behaviour it guards (#15 — stale hold_until must not show
             once the hold is released) regressed completely. A green suite
             reported this as fine.
Expected:    An assertion that stops being meaningful should fail, not pass.
Status:      open — snagged 18 Jul. Both assertions repointed at the live
             label that same day, so the pair is load-bearing again; this
             snag is about the GENERAL pattern, not that instance.
Notes:       The real issue is ALTITUDE, not tests-vs-no-tests. The same
             behaviour is already tested at model level
             (DisplayPredicateTest.php:68 calls showsHoldUntil() directly):
             that test pins the logic, costs nothing to maintain and cannot
             rot. The view-level pair buys one extra thing — proof the
             template actually USES the predicate rather than rendering
             hold_until unconditionally — but pays for it in copy-fragility
             plus the silent-decay hazard above.
             A test's value is what it tells you when it fails. One of these
             said "you renamed a label" (near-zero information); the other
             said nothing when it should have shouted. Neither was earning
             its keep at that altitude.
             SCOPE: 22 assertDontSee assertions across 8 Feature test files
             (D16/CaseOversightTest 4, D16/DisplayPredicateTest 10,
             D16/ExhaustedStanceUiTest 1, Phase6/CaseCreateTest 2,
             Phase6/CaseIndexTest 1, Phase6/CaseShowTest 1,
             Phase65/PropertyIndexTest 1, DashboardOnboardingTest 2).
             Not all are copy-anchored and not all are at risk — the count is
             the search space, not the defect count.
Fix:         Audit the assertDontSee assertions above. For each, ask whether
             the string it names still exists ANYWHERE in the app; if not,
             the assertion is already vacuous. Then per case either:
             (a) DELETE it where the behaviour is already covered by a
                 model/predicate-level test — the logic stays pinned and the
                 fragile duplicate goes; or
             (b) RE-ANCHOR it to something stable — a data-testid on the
                 element, or the rendered VALUE (e.g. the date) rather than
                 the label — so copy can change freely while the wiring stays
                 tested.
             Avoid the state this snag describes: keeping fragile assertions
             AND trusting them.
             Cheap partial win: a CI check that greps each assertDontSee
             string literal and fails if it appears nowhere in
             resources/views — catches vacuous assertions automatically.
--------------------------------------------------------------------
#27
Page:        Email verification — clicking the verify link in a browser with
             no session bounces the user to /login instead of just verifying
             them. Surfaced 2026-07-25 in normal use.
Severity:    minor — friction, not a defect. Verification still completes
             after the extra login. Bites cross-browser flows most.
Saw:         Register in Chrome (session lives in Chrome). Outlook opens the
             verification email in Edge, which has no session. The verify
             route sits behind `auth` middleware (routes/auth.php:38), so Edge
             — a guest — is redirected to /login before the controller runs.
             The user is asked to "say who they are" even though the signed
             link already carries their id + hash and identifies them.
Expected:    A signed verification link clicked from the user's own inbox
             should verify AND log them in, landing them on the dashboard in
             whatever browser opened it — no login step.
Status:      open — snagged 25 Jul. Deferred deliberately (discussed, chose
             not to build yet). Present-day workaround: copy the link out of
             Outlook and paste it into Chrome, where the session already
             exists.
Notes:       Two things are conflated in the current UX: (1) VERIFYING the
             email (the link does this alone, no session needed) and (2)
             starting a browser SESSION (why the login prompt appears). The
             prompt is about the session, not doubt about the link.
             Half-built already: VerifyEmailController.php:34-57 has a
             "if not logged in, validate signature and Auth::login" branch,
             but it is unreachable — the `auth` middleware bounces guests
             first, and the route's EmailVerificationRequest assumes an
             authenticated user (its authorize() dereferences $this->user()),
             so it would error for a guest even past the middleware.
             Security note: this converts the emailed link into something that
             grants a session on possession. Acceptable for a private beta —
             the route already has `signed`, single-use (used_at), and
             `throttle:6,1`. Reconsider posture before opening registration
             wide / at prod scale, where forwarded-inbox risk grows.
             OBSERVED ON PROD 2026-08-02 — a WORSE failure mode than the
             one recorded above. Registered in one browser, opened the
             verify link in another, and got a hard
             "403 This action is unauthorized" — not the /login redirect
             this snag originally described.
             Browsers used: admin session in CHROME, registration in
             FIREFOX, verify link opened in EDGE. Edge was believed to be
             session-free.
             CAUSE NOT YET ESTABLISHED — an earlier note here blamed "the
             browser was signed in as admin"; that is RETRACTED, the admin
             session was in Chrome, not Edge.
             What the code does establish: the route sits inside
             Route::middleware('auth') (routes/auth.php:38-44) with
             `signed` + `throttle:6,1` on top, and there is no custom
             Authenticate override in bootstrap/app.php. So a genuine
             guest gets a 302 to /login — `auth` CANNOT emit a 403. Both
             403 sources on this route sit downstream of `auth`:
               - `signed`                     -> "Invalid signature."
               - EmailVerificationRequest::authorize() -> "This action is
                 unauthorized."  <- the wording actually seen
             Therefore the request PASSED auth, which means Edge held a
             session for some user whose id did not match the link (a
             stale login from earlier testing, or a remember-me cookie,
             would do it). That contradicts the recollection, so it is
             recorded as an open question rather than a conclusion.
             TO SETTLE IT: read storage/logs/laravel.log on prod at the
             time of the 403. AuthorizationException = id mismatch;
             InvalidSignatureException = the link itself was rejected,
             which would instead point at the mail client mangling the
             URL. Do this before designing the fix — the two causes need
             different fixes.
             So the half-built guest branch at VerifyEmailController.php:
             34-57 is unreachable TWO ways, not one:
               - truly logged out -> `auth` middleware redirects to /login,
                 controller never runs;
               - logged in as ANYONE ELSE -> authorize() 403s, controller
                 never runs.
             The second path is the more likely one in real use (people
             stay signed into things), and a 403 reads as BROKEN rather
             than as a step to complete. Raises the priority of this snag
             from friction to something a real user would report as a bug.
             Verification itself still completed fine once the link was
             opened in the original browser — this is UX, not a broken
             verification path.
Fix:         Move verify-email/{id}/{hash} out of the `auth` group so a guest
             click reaches the controller's existing login branch, AND change
             the action off EmailVerificationRequest (whose authorize()
             needs a logged-in user) to a plain Request — the branch already
             does its own findOrFail + hasValidSignature; keep `signed` +
             `throttle` on the route. Add a test for the guest/cross-browser
             path (verify while logged out -> marked verified + session
             started + lands on dashboard). Keep the already-logged-in path
             intact.
--------------------------------------------------------------------
#28
Page:        Login + Register forms — the email field is blank on every fresh
             load, so it must be retyped each time. Nice-to-have: default it
             on non-prod for testing. Surfaced 2026-07-25.
Severity:    trivial — dev-convenience only. No user-facing defect.
Saw:         Both auth/login.blade.php (email input ~line 35) and
             auth/register.blade.php (~line 38) use value="{{ old('email') }}",
             which only repopulates after a failed submit — a fresh form is
             empty. Repeated test logins/registrations mean retyping the same
             address every time.
Expected:    On non-production boxes (local + gafol), the email field
             pre-fills with a configured test address; prod stays blank and a
             failed submit still shows whatever was typed.
Status:      open — snagged 25 Jul. Deferred. Applies to BOTH forms.
Notes:       Do NOT hardcode a personal email into the blades — it would ship
             to prod and into GitHub. Drive it from an env var so it stays out
             of git (.env is gitignored) and off prod.
Fix:         Add config key app.dev_default_email => env('DEV_DEFAULT_EMAIL')
             (via config, NOT raw env(), because gafol runs config:cache).
             In both forms use:
               value="{{ old('email', app()->environment('production')
                        ? null : config('app.dev_default_email')) }}"
             Then set DEV_DEFAULT_EMAIL in local (and gafol) .env only.
             Prod has no such var -> field stays blank there.
--------------------------------------------------------------------
#29
Page:        Login — the "Members Only" redirect banner is unnecessary and a
             bit intimidating, especially when reached from the verify link.
             Surfaced 2026-07-25.
Severity:    trivial — tone / UX polish. No functional defect.
Saw:         auth/login.blade.php:14-19 shows a yellow alert-warning reading
             "Members Only / We reserve these pages for members, so please
             login or register" whenever session has 'url.intended' — i.e.
             any redirect-to-login from a guarded page. The verify-email
             bounce (see #27) is exactly that case, so a user clicking their
             own verification link is met with a WARNING-styled gate message.
Expected:    A calm, welcoming prompt (or nothing) — the warning colour +
             "Members Only" framing reads as a rebuke for what is a normal
             "please sign in to continue" moment.
Status:      open — snagged 25 Jul. Related to #27 (the redirect that
             triggers it). Fixing #27 removes this banner from the verify
             path specifically; this snag is the broader tone issue.
Fix:         Soften it: drop "Members Only", switch alert-warning -> a
             neutral alert-info/light, and reword to something like "Please
             sign in to continue." Optionally suppress it entirely on the
             verify path once #27 lands. Copy-only + class change; no logic.
--------------------------------------------------------------------
#30
Page:        Contact Us (contact.create/store) + admin reply
             (admin.contact-messages.*). Reviewed 2026-08-01.
Severity:    minor — capability gap / expectation mismatch, NOT a defect.
             Everything present works; the question is whether it's enough.
Saw:         Contact Us is a stored-message box with a ONE-WAY, ONE-SHOT
             email reply — not a two-way email conversation.
             - Inbound (user->us): ContactMessageController::store writes a
               contact_messages row only. NO email is sent on submit; auth-
               only route, so the sender is a known user.
             - Outbound (us->user): AdminController::contactMessageReply sends
               exactly ONE real email (ContactReply mailable, via Mailgun) and
               is guarded against a second (`if ($contactMessage->admin_reply)`).
             - It does NOT receive email back: ContactReply goes out
               from noreply@renters.rent (ContactReply.php:19) with no Reply-To
               and no inbound webhook for contact mail. A user hitting "reply"
               in their inbox lands nowhere. No threading.
Expected:    Decide the intended shape and make the UX match it. Either
             (a) confirm one-shot-reply is the design and set expectations
                 (e.g. wording in the reply email: "this mailbox is not
                 monitored — reply via the site"), or
             (b) make it a real two-way thread.
Status:      open — snagged 01 Aug. DECISION TAKEN 01 Aug: rebuild rather
             than patch — option (b), a real two-way thread. The one-shot
             reply is not the design we want on a platform whose whole
             proposition is that correspondence does not get lost.
             INTERIM MITIGATION if the rebuild is delayed: set a replyTo
             on a monitored address, so a user reply reaches a human
             instead of vanishing. That is a holding measure, not the fix.
             Distinct from the LLCS case correspondence (#4, which IS full
             send-receive with Mailgun inbound binding replies onto cases).
             Contact Us deliberately reuses NONE of that machinery.
Notes:       Not urgent while contact volume is low and admin can eyeball the
             list. The trap is silent: a user who replies to our answer
             reasonably assumes it reached us; it did not. If a two-way thread
             is wanted it is a larger build (inbound routing/threading), so
             the cheap interim is (a) — a Reply-To that goes somewhere real
             OR an explicit "do not reply" note in ContactReply. The admin
             one-reply guard also means a follow-up question can't be answered
             on the same thread even from our side.
Fix:         Minimum: add a monitored Reply-To (or a "please reply via the
             Contact form / this address is unmonitored" line) to the
             ContactReply mailable so a user reply isn't silently lost.
             Larger option (only if a real conversation is wanted): route
             inbound contact mail (mirror the MailgunInboundController /
             verify.mailgun.signature pattern), thread replies onto
             contact_messages, and drop the single-reply admin guard.
--------------------------------------------------------------------
#31
Page:        docs/deploy-checklist.md lines 24 and 60 — the checklist names
             a DNS domain that does not exist. Surfaced 2026-08-01 during
             the mail identity audit.
Severity:    minor as it stands (doc only, nothing broken today) but the
             FAILURE MODE is functional and silent. Doc-only fix, low risk.
Saw:         Both lines give inbox.renters.rent as the production
             MAILGUN_INBOUND_DOMAIN — line 24 in prose ("Production values:
             cases@mg.renters.rent and inbox.renters.rent"), line 60 inside
             a copy-paste .env block. That domain does not exist in the
             renters.rent DNS zone: no MX, no A record. Production actually
             uses mg.renters.rent.
Expected:    The checklist states the value production actually runs.
Status:      open — snagged 01 Aug. Flagged for a call on whether to fix
             now or post-trial; it is a two-line doc edit either way.
Notes:       Why this is worse than an ordinary stale doc: a rebuild
             following the checklist would set the dead domain, and the
             CaseNotice constructor guard would NOT catch it. That guard
             tests PRESENCE, not validity (CaseNotice.php:49-57 — blank()
             only), so a non-existent domain sails through, every landlord
             letter goes out with a Reply-To at an NXDOMAIN address, and
             every landlord reply hard-bounces. Nothing in the app would
             report it — the letters send fine; only the replies die.
             The value also appears (harmlessly) in .env.example:78, which
             is COMMENTED OUT, and in phpunit.xml:30 as a test-env value,
             and across four design docs including two archived copies.
             Only the deploy-checklist occurrences are load-bearing.
Fix:         Change both occurrences to mg.renters.rent. Consider also
             whether the CaseNotice guard should validate the domain
             resolves rather than merely being non-blank — separate call,
             don't fold it into the doc fix.
--------------------------------------------------------------------
#32
Page:        app/Mail/ContactReply.php:19 — hardcoded apex sender.
             Surfaced 2026-08-01 during the mail identity audit.
Severity:    minor — no user-visible defect today. Discipline/consistency
             gap with a real deliverability consequence (see #33).
Saw:         ContactReply sends from: 'noreply@renters.rent' as a string
             literal in the Envelope. It is the ONLY hardcoded sender and
             the ONLY apex sender in the app. Everything else routes
             through config: CaseNotice reads
             services.mailgun.cases_from_address, and the tenant notices
             plus all of Laravel's auth mail fall through to
             config('mail.from.address').
Expected:    Sender identity comes from per-environment config, and a
             missing value fails loudly — the discipline config/services.php
             already applies to the case-mail keys.
Status:      open — snagged 01 Aug. Replacement code already drafted;
             NOT applied. Do not build from this entry.
Notes:       Two consequences. (1) It bypasses the fail-loud pattern:
             services.php:23-25 deliberately gives cases_from_address and
             inbound_domain no default so a misconfigured environment
             throws rather than sending production identity through the
             wrong domain. ContactReply cannot fail that way — it always
             has a value, so it cannot be caught. (2) It sends the same
             apex production identity from LOCAL and STAGING, which is
             exactly what that discipline exists to prevent.
             This entry is also what forces the apex SPF record — see #33.
Fix:         Add contact_from_address to the services.mailgun block with
             NO default; read it in ContactReply; throw on blank in the
             constructor as CaseNotice does; add tags: [app()->environment()]
             to the Envelope so a misrouted send shows up in Mailgun's
             per-domain log. Set MAILGUN_CONTACT_FROM_ADDRESS per
             environment. Then #33 becomes actionable.

             OBSERVED ON PROD 2026-08-02 — the visible symptom, confirmed
             in a real inbox. A Contact Us reply arrives in Gmail showing:
               noreply=renters.rent@mg.renters.rent
               on behalf of noreply@renters.rent
             Cause: two different sender addresses on the same message.
             The From: header is the hardcoded apex noreply@renters.rent,
             while the envelope sender / Return-Path is Mailgun's VERP
             address on the domain it actually controls (the local part
             noreply=renters.rent encodes the original address). Gmail
             prints "on behalf of" whenever those two domains differ.
             NOT an error and NOT a misconfiguration — DMARC should still
             pass, because alignment defaults to RELAXED and both share
             the organisational domain renters.rent. Mail arrives fine.
             Why it belongs here rather than as its own snag: the odd
             display exists BECAUSE ContactReply hardcodes an apex sender
             while everything actually leaves via mg.renters.rent. Fixing
             this entry (move it to contact@mg.renters.rent via config)
             makes From and Return-Path match exactly and the "on behalf
             of" disappears on its own. No separate work.
             Also strengthens #33: once this lands, the apex SPF record
             added 1 Aug is no longer needed and the apex can tighten to
             v=spf1 -all.

             CORROBORATED BY CONTRAST 2026-08-02 — headers read from a
             landlord letter delivered to Outlook show Sender: and From:
             BOTH cases@mg.renters.rent, i.e. matching, and that message
             displays no "on behalf of" anywhere. Same infrastructure,
             same signing key, one difference: the sender domains agree.
             That isolates the display artefact to the apex sender alone
             and confirms the mechanism recorded above, so the diagnosis
             is no longer reasoning from first principles.
             Still NOT read: the Contact Us message's own headers. The
             claim that it passes DMARC on relaxed alignment remains
             inference. Cheap to settle — read Authentication-Results on
             any Contact Us reply — and worth settling before #33 tightens
             the apex on the strength of it.
             Severity confirmed COSMETIC, not a delivery risk: this path
             is about trust and appearance, not whether mail arrives.
--------------------------------------------------------------------
#33
Page:        DNS — the apex SPF record published 2026-08-01 is a temporary
             shape, and nothing in the zone says so.
Severity:    minor — correct and honest today; becomes wrong silently once
             #32 is done.
Saw:         renters.rent now publishes v=spf1 include:mailgun.org ~all at
             the apex. That is honest ONLY because ContactReply still sends
             from noreply@renters.rent (#32). The record authorises Mailgun
             to send as the apex.
Expected:    Once #32 lands and all sending is under mg.renters.rent, the
             apex should authorise nothing: v=spf1 -all. Consider null MX
             (RFC 7505) too, if the apex is to accept no mail either.
Status:      open — snagged 01 Aug. BLOCKED ON #32; do not tighten before
             it, or Contact Us replies start failing SPF.
Notes:       Recorded because a bare include:mailgun.org at the apex looks
             PERMANENT to anyone reading the zone later — including us in
             six months. There is nothing in DNS that can carry the
             reasoning, so it has to live here. The full before/after of
             the 01 Aug change set is in docs/DNS records old values.txt.
             Both DMARC policies published that day are p=none — monitoring
             only, nothing rejected or quarantined. Tightening the policy
             is a separate decision from tightening SPF.
Fix:         After #32: change the apex TXT to v=spf1 -all, and decide on
             null MX. Record the change in the DNS file alongside the 01 Aug
             entry, with the reasoning, so the next reader sees why the
             record moved twice.
--------------------------------------------------------------------
#34
Page:        CLAUDE.md, Mail section — a standing rule that contradicts the
             code. Surfaced 2026-08-01 during the mail identity audit.
Severity:    minor — no runtime impact. Misleads the reader on a
             load-bearing safety property, which is worse than it sounds.
Saw:         CLAUDE.md states the config/services.php Mailgun keys carry
             production defaults of mg.renters.rent. They do not. Verified:
             MAILGUN_DOMAIN, cases_from_address and inbound_domain are all
             bare env() calls with NO default, by explicit design — the
             comment at services.php:23-25 says so in terms. Only `endpoint`
             carries a default (api.eu.mailgun.net).
Expected:    The working agreement describes what the code does.
Status:      open — snagged 01 Aug. Doc-only fix.
Notes:       Worth correcting rather than leaving: the rule as written
             implies a safety net that does not exist. Someone trusting it
             would assume a missing MAILGUN_CASES_FROM_ADDRESS quietly
             falls back to production values — when in fact CaseNotice
             throws and no letters send at all. Those are opposite
             failure modes, and the real one is the safer of the two.
             A standing rule that contradicts the code is worse than no
             rule, because it is consulted instead of the code.
Fix:         Rewrite the CLAUDE.md Mail bullet to state the actual design:
             the case-mail keys are REQUIRED per environment with no
             defaults, and CaseNotice throws if either is blank. Keep the
             per-environment routing table (prod/preprod Mailgun
             mg.renters.rent, staging sandbox outbound-only, local Mailpit)
             — that part is accurate.
--------------------------------------------------------------------
#35
Page:        Local .env — MAIL_FROM_ADDRESS is a third-party domain.
             Surfaced 2026-08-01 during the mail identity audit.
Severity:    trivial — no production impact. Worth fixing because the
             failure mode is embarrassing rather than costly.
Saw:         Local MAIL_FROM_ADDRESS is jcwatts99@outlook.com. That is the
             global default sender, so it governs every mailable that does
             not set its own from — the tenant notices and all of Laravel's
             auth mail (verification, password reset).
Expected:    Development mail sends from a domain we control, or from a
             reserved test domain that cannot resolve.
Status:      open — snagged 01 Aug. Local only; no production change.
Notes:       Normally harmless because local mail goes to Mailpit and never
             leaves the machine. The hazard is if dev mail ever escapes to
             a real recipient — a misconfigured MAIL_MAILER, a copied .env,
             a box pointed at Mailgun by accident. Then we would be sending
             mail claiming to be from outlook.com over infrastructure with
             no authority for that domain: fails DMARC alignment outright,
             and spoofs Microsoft's domain from ours. Not a risk we are
             taking today, but not one worth carrying either.
Fix:         Change local MAIL_FROM_ADDRESS to something on mg.renters.rent
             (e.g. dev@mg.renters.rent) or a reserved .test domain, which
             cannot resolve and so cannot escape anywhere. Check the same
             value on gafol while at it.
--------------------------------------------------------------------
#36
Page:        Mailgun account tier — OPEN QUESTION, do not action.
             Raised 2026-08-01.
Severity:    open question, not a snag. No decision taken. Recorded so it
             is not re-derived from scratch later.
Saw:         The account is on the free tier, which sends from SHARED IP
             pools. Shared pools are a structural deliverability risk —
             your reputation is partly other people's behaviour. It bites
             hardest at Microsoft, which weighs IP reputation alongside
             domain reputation more heavily than Gmail does.
Expected:    A deliberate decision on tier, made on answers rather than
             assumption.
Status:      open — QUESTION ONLY. Nothing to build. Do not act on this
             entry without a decision from Charlie.
Notes:       Relevant history: #8 and #25 record two separate occasions
             where sends were silently blocked (Gmail 550 / 5.7.1 on the
             sandbox domain). Those were sandbox-reputation failures rather
             than shared-pool failures, but they are the same class of
             problem — deliverability we do not control and cannot see.
             Alternative under consideration: Postmark. Subject to
             CHECKING DATA RESIDENCY first — this platform holds UK tenant
             data, and where mail content is processed and retained is a
             condition of the choice, not a detail.
Fix:         Before paying for Basic 10k (£12/mo), two PRE-SALES questions
             to Mailgun support:
             (a) does a paid account send from a DIFFERENT shared IP pool
                 than a free account? (If not, paying buys volume, not
                 deliverability — which changes the whole case.)
             (b) does the "1 sending domain" limit count the gafol sandbox
                 domain? If it does, Basic BREAKS STAGING and is unusable
                 at any price — staging must keep its own outbound path.
             Answer both before spending anything.

             ANSWERED 2026-08-02 — Mailgun (Sinch) support, question (a):
             "Different plan tiers don't come with higher reputation IP
             pools." Paying buys volume and features, NOT deliverability.
             That removes the entire deliverability case for upgrading,
             which was the reason this entry existed.
             DECISION: do not upgrade on deliverability grounds. Revisit
             only if VOLUME or a feature forces it — at which point (b),
             the sending-domain count, still needs answering because it
             could break staging. (b) remains UNANSWERED; it is moot
             while we are not buying.
             Do not buy a dedicated IP either. Dedicated IPs start with
             no reputation and need sustained volume to warm up; at a
             few letters a week one would perform WORSE than the shared
             pool. Shared is correct here and will be for a long time.
             Corroborating evidence, same day: landlord letters to
             Outlook scored SCL:1 BCL:0 dest:I (Inbox) from the shared
             pool, with SPF/DKIM/DMARC all passing and aligned. The
             structural risk recorded above is real but is NOT currently
             biting — see environment-state.md, 2 Aug header read.
             Also from the reply, worth keeping for #25: Mailgun will
             investigate a delivery failure only if given the Message-ID
             of an event that names one of THEIR IPs as the cause. That
             tells us what to retain when failure detection is built.
             Status: (a) closed. Tier question closed. (b) parked.
--------------------------------------------------------------------
#37
Page:        Post-verification welcome banner (dashboard.blade.php) —
             does not appear. Surfaced 2026-08-02 on the FIRST real
             registration on prod, the day after it shipped.
Severity:    minor — cosmetic, no functional impact. But it is a feature
             that shipped 1 Aug and has never once worked in real use,
             which is the interesting part.
Saw:         Registered on prod, verified, landed on the dashboard —
             and the green "Welcome — and thanks for registering!"
             banner did NOT show.
Expected:    A first-time user completing verification sees the welcome
             banner on the dashboard they land on.
Status:      open — snagged 02 Aug. Introduced by commit 79989e9 on
             feature/admin-unverified-users, merged in 7507a72 and
             deployed to prod the same day.
Notes:       ROOT CAUSE — VerifyEmailController.php:31:
                 return redirect()->intended(
                     route('dashboard', absolute: false).'?verified=1');
             intended() uses its argument only as a FALLBACK. If the
             session holds a url.intended — which it does, because the
             user hit /dashboard while unverified and was bounced — it
             redirects THERE instead and discards the ?verified=1
             entirely. The blade reads request()->boolean('verified')
             (dashboard.blade.php:10), so no query param means no banner.
             The user lands on the dashboard because that is what was
             intended; just without the flag.
             SECOND, INDEPENDENT GAP in the same file: the not-logged-in
             branch at line 57 redirects to route('dashboard') with NO
             ?verified=1 at all. So even once #27 is fixed and that
             branch becomes reachable, the banner still would not fire.
             Both branches need to carry the signal.
             Worth noting the class of bug: a query-string flag is
             fragile because it only survives if the redirect target is
             exactly the one you predicted. It also leaves ?verified=1
             sitting in the address bar, where a refresh or a shared link
             re-triggers a "welcome, you just registered" message for
             someone who did not just register.
Fix:         Stop carrying this in the URL. Flash it to the session
             instead — redirect()->intended(...)->with('verified', true)
             on the logged-in branch, the same ->with() on the guest
             branch at line 57, and change the blade to read
             session('verified') rather than request()->boolean().
             That survives ANY redirect target, works on both branches,
             and leaves nothing in the address bar. Add a test asserting
             the banner shows after a real verify redirect — there is
             currently none, which is why this shipped.
--------------------------------------------------------------------
#38
Page:        Property city is stored and rendered exactly as typed —
             no capitalisation of the leading character. Surfaced
             2026-08-02 reading a real landlord letter on prod.
Severity:    minor — cosmetic, but it lands in the one place that is
             meant to look formal.
Saw:         Notice 1 to the landlord renders the property address as
             "66 Pond Road, reading, RG1 5RD". The tenant typed the
             town in lower case at property creation and nothing
             normalised it. The same string appears in the letter
             SUBJECT line, so it is visible before the message is even
             opened.
Expected:    Town renders capitalised — "Reading" — in a formal notice
             citing the Landlord and Tenant Act 1985.
Status:      open — snagged 02 Aug.
Notes:       Why it is worth fixing rather than tolerating: this letter
             is the product's core artefact and its whole job is to be
             taken seriously by a landlord. A lower-case town name reads
             as an unproofed automated mailshot, which is exactly the
             impression the letter must not give. The tenant typed it,
             but the landlord sees OUR letter.
             Precedent already set: registration and profile-update
             normalise email case on input (1 Aug). This is the same
             class of problem in the same class of field, and the same
             answer applies.
             Scope note: address_line1/2 have the identical exposure and
             are equally visible in the subject line. Fixing city alone
             leaves "66 pond road" possible. Postcode is a third case —
             it should be UPPER, not title case.
             Beware naive ucwords() on UK addresses: it produces
             "Stratford-Upon-Avon", "Kings Lynn" losing "King's", and
             mangles "St Leonards-on-Sea". Title-casing a free-text
             address field is not as simple as it looks — decide
             deliberately how far to go.
Fix:         Normalise on input in PropertyController (validation rules
             at :112, assignment at :60 and :95 — store AND update
             both). Trim, then apply the chosen capitalisation to city
             and address lines, and strtoupper the postcode. Add a test
             per field asserting the stored value, mirroring the email
             normalisation tests. Decide separately whether to correct
             EXISTING rows — letters already sent are frozen evidence
             and must not be altered, but the property record can be.
--------------------------------------------------------------------
#39
Page:        resources/views/cases/preview.blade.php — the D13 preview
             does not show that photos are attached. Surfaced
             2026-08-02.
Severity:    minor — no data loss, but it undermines what the preview
             is FOR.
Saw:         The two-step create flow (store -> preview -> confirm)
             renders the notice the landlord will receive so the tenant
             can check it before anything is sent. Staged photos are
             held at that point and WILL be attached to notice 1, but
             the preview makes no mention of them — no filenames, no
             count, no thumbnails. Nothing in preview.blade.php
             references photos or attachments at all.
Expected:    The preview shows what is about to be sent, attachments
             included — at minimum a count and the filenames, matching
             what _message_card.blade.php already renders after the
             fact.
Status:      open — snagged 02 Aug.
Notes:       The preview is the tenant's ONLY chance to check the
             letter before it goes to their landlord, and photos are
             the one part they cannot take back once sent. A tenant who
             attached the wrong photo — of the wrong room, or one that
             happens to show a family member — has no way to notice
             from this screen.
             It also fails the other way: a tenant whose upload was
             silently rejected sees a preview identical to a successful
             one, and only learns the letter went out bare afterwards.
             That failure mode is live TODAY, given the 2M server limit.
             The data needed is already in the session payload the
             preview reads — count($payload['photos']) plus each
             original_filename. No new query, no new state.
Fix:         Render the staged photo count and filenames in
             preview.blade.php from the session payload, in the same
             shape _message_card.blade.php uses (filename + KB). Add a
             test asserting the preview names each staged file. Say
             explicitly when there are none, so "no photos attached" is
             visible rather than inferred from absence.
--------------------------------------------------------------------
#40
Page:        CaseController::store validation (:359-360) — photo limits
             are per-file only, with no combined-size rule, and the
             per-file ceiling is too small for ordinary phone photos.
             Surfaced 2026-08-02 attempting the FIRST real attachment
             test on prod.
Severity:    MAJOR. Two distinct defects, one of which destroys tenant
             input with no warning. Photographs are the strongest
             evidence a tenant has and this is their only chance to
             attach any.
Saw:         (a) A 3.22MB PNG — a completely ordinary phone-photo size —
             was rejected with "The photos.0 failed to upload." That
             string is Laravel's `uploaded` rule firing because PHP
             rejected the file before validation ran. It names no size,
             no limit and no remedy, and "photos.0" is meaningless to a
             tenant. Proven on prod: 162KB succeeds, 3.22MB fails.
             (b) The form permits 6 files at max:2048 each — up to 12MB
             — but the server's effective post_max_size is 8M (see #41).
             PHP discards an oversized POST ENTIRELY, including the CSRF
             token, so the tenant gets a 419 "page expired" and loses
             the description they typed. NOT YET REPRODUCED — inferred
             from the measured limits; confirm before relying on it.
Expected:    A tenant attaching normal phone photos either succeeds, or
             is told clearly and specifically what to change, BEFORE
             anything they have typed is lost.
Status:      open — snagged 02 Aug. Hosting half RESOLVED 09 Aug (#41
             closed: PHP Selector, limits now 8M/16M and verified).
             (b) IS NOW CLOSED BY HEADROOM: the form's own maximum
             (6 x 2MB = 12MB) fits inside post_max_size 16M, so the
             submission-destroying 419 can no longer be constructed. It
             was never reproduced and now cannot be.
             (a) REMAINS OPEN and is entirely ours: max:2048 at
             CaseController.php:360 refuses the 3.22MB photo before PHP
             ever objects. Only the error message improved.
             SUPERSEDED IN PART — docs/attachment-policy-design.md now
             carries the decisions (3-attachment ceiling as an admin
             setting, 4MB per file, no combined-size rule needed since
             3 x 4MB is safe by construction, sweep letters never attach).
             Work that note, not the Fix line below, for everything except
             the error-message wording.
Notes:       Never caught by tests because the suite fabricates
             UploadedFile objects in memory — they never touch PHP's
             upload limits or a real filesystem. Green tests could not
             have found this. It is a good argument for exercising file
             paths on a real box before trusting them.
             The attachment path itself WORKS: proven end to end on prod
             the same day (case WS2ZPP), original filename preserved,
             and the letter scored SCL:1 BCL:0 dest:I — a small
             attachment cost nothing in deliverability. Untested at the
             top end: no multi-megabyte letter has been spam-scored,
             because the limits above prevent sending one.
             Design tension to resolve, not just a number to raise:
             6 files x 5MB = 30MB, over Mailgun's 25MB message ceiling.
             Raising limits alone trades a visible failure for a silent
             one. The durable answer is resize-on-upload — keep the
             tenant's ORIGINAL untouched on disk as the evidential
             record, attach a compressed derivative to the letter. That
             accepts any phone photo, keeps letters small, and preserves
             the evidence. Note it means the bytes the landlord receives
             are not the bytes held as evidence; that is a deliberate
             decision and should be recorded as one, not drifted into.
Fix:         Immediate, independent of hosting: add a combined-size
             validation rule below whatever the server actually accepts,
             so oversize is refused with a clear message instead of
             destroying the submission; and override the `uploaded` and
             `max` messages for photos.* to name the file and the limit
             in plain words. Then decide on resize-on-upload separately
             — that one is a design call, not a patch.
--------------------------------------------------------------------
#41
Page:        Hosting — Plesk PHP Settings for renters.rent do not reach
             the running PHP. Surfaced 2026-08-02 while diagnosing #40.
Severity:    MAJOR as a trust problem, whatever the upload numbers turn
             out to be. A control panel that displays and saves settings
             it does not apply invalidates anything configured there.
Saw:         Domain renters.rent, PHP 8.4.23 as FastCGI application.
             Set and APPLIED in the domain's PHP Settings page:
             upload_max_filesize 8M, post_max_size 16M. phpinfo for the
             same domain, after a forced reload, still reports Local
             Values of 2M and 8M. Confirmed functionally, not just from
             phpinfo: the 3.22MB upload still fails after applying.
             Before those changes the panel displayed 32M for BOTH
             values — and 32M is not an option in either dropdown, which
             tops out at 16M. So the page was showing a stored value it
             cannot itself set and which was never in effect.
Expected:    The domain PHP Settings page is authoritative for the
             domain, or says plainly that it is not.
Status:      CLOSED 2026-08-09 — HUK answered: the settings are NOT locked
             at plan or server level; the domain uses CloudLinux PHP
             SELECTOR, not the Plesk PHP Settings page, so everything set
             on that page was inert. HUK raised the limits via the
             Selector. VERIFIED at our end, both from phpinfo Local Values
             (upload_max_filesize 8M, post_max_size 16M — Local and Master
             agree, so it is set in the alt-php ini rather than a
             per-directory override) AND functionally: the 3.22MB upload
             now reaches Laravel and is refused by our own max:2048 rule
             instead of PHP's "failed to upload". That change of error
             message is the proof the setting is live.
             STANDING CONSEQUENCE — the Plesk PHP Settings page is NOT
             authoritative for this domain, for ANY directive, not just
             uploads. Anything ever set there (memory_limit,
             max_execution_time, ...) was equally ignored. Use PHP
             Selector; verify from phpinfo, never from either panel.
             HUK did not answer the "what else on that panel is
             ineffective" question directly — the answer is everything.
Notes:       Ruled out at our end: no php_value in public/.htaccess
             (stock Laravel rewrite rules only), no .user.ini anywhere
             in the application, no application php.ini.
             The consequence reaches past uploads. deploy-checklist.md
             and huk-laravel-site-install-recipe.md both assume Plesk
             PHP settings can be read and set at face value. Until this
             is understood, any value on that page is unverified — and
             most of them fail far less visibly than an upload does.
             Verify effective values from phpinfo, not the panel.
Fix:         Hosting answer required: are these directives locked at
             service-plan or server level; what maxima are available;
             and is that panel meant to be authoritative. If it is not,
             record in the install recipe which settings must be
             verified via phpinfo rather than trusted from the panel.
--------------------------------------------------------------------
#42
Page:        /landlords ("For Landlords") — the trading identity block is
             unfilled, and no correspondence address exists to fill it
             with. Raised 2026-08-02 building the page.
Severity:    functional — BLOCKS PUBLISHING the page, and carries a legal
             disclosure requirement plus a personal-safety decision. Not
             a code defect; a purchase and a decision.
Saw:         The page renders "[NAME]" and "[ADDRESS]" as literal
             placeholders. The business is a sole trader, so there is no
             company number or registered office to fall back on.
Expected:    The page names the trader and gives an address at which
             documents can be served, as required of a sole trader using
             a business name — and does so without exposing a home
             address.
Status:      open — snagged 02 Aug. Page is committed and in the nav but
             MUST NOT be deployed until this is resolved; it currently
             renders bracketed placeholder text to the public.
Notes:       Why an address is not optional: a sole trader trading under
             a business name must disclose the trader's own name and an
             address for service of documents, on the website as well as
             on correspondence. Omitting it is not a tidiness choice.
             Why NOT the home address: this is the one page in the
             product read by people who have just received a formal
             notice about their own property, some of whom will be
             angry. A tenant-facing page has a sympathetic audience; this
             one does not. A mail-forwarding or virtual office address is
             roughly £20-50/year and removes the exposure entirely.
             Treat the cost as part of launching, not as an extra.
             Do not publish the page with placeholders visible, and do
             not "temporarily" substitute the home address — temporary
             addresses on public pages are archived, cached and scraped
             within days, and cannot be withdrawn afterwards.
             SEPARATE CHECK, same block: the ICO reference published on
             the page is ICO:00014275530 — eleven digits. ICO public
             registration references are normally of the form ZA123456 /
             ZB123456, and an eleven-digit number looks more like an
             account or payment reference. Confirm the number returns the
             entry when searched on the ICO public register. A number
             that resolves to nothing is WORSE than no number: a landlord
             who checks it and finds nothing concludes the opposite of
             what the page intends.
             DECISION 2026-08-02 (Charlie) — for the ONE-OFF FAMILY TRIAL
             only, the page carries no trader name and no correspondence
             address. "Who we are" now reads simply that renters.rent is a
             new service developed to aid tenants and landlords in their
             joint pursuit of rental property improvement. The bracketed
             placeholders are gone, so the page is deployable for the
             trial.
             THIS SNAG STAYS OPEN. The decision is scoped to the trial and
             does NOT carry to public launch, where the name and a service
             address become required disclosures again. The trial audience
             is a handful of known family landlords; the launch audience is
             strangers who have just received a formal notice about their
             own property. Different exposure in both directions.
             For accuracy: the page is reachable by anyone at
             renters.rent/landlords during the trial — the registration
             gate covers signing up, not public pages. The decision is that
             this is acceptable at trial scale, not that the page is
             private.
Fix:        Before public launch: decide and buy a service address, set the
             trader name and address in
             resources/views/landlords.blade.php and remove the trial-
             wording comment; verify the ICO reference on the public
             register and correct it if it is the wrong identifier.
             Consider whether the same address belongs in the site footer
             and privacy notice, which have the same disclosure exposure.
             Add to pre-flip-checklist.md so launch cannot pass without it.
--------------------------------------------------------------------
#43
Page:        Create-case form, photo input (cases/create.blade.php:118) —
             selecting photos in two separate goes silently discards the
             first. Surfaced 2026-08-09 on prod.
Severity:    functional — SILENT EVIDENCE LOSS. The tenant believes files
             are attached that are not. Photographs are the strongest
             evidence a tenant has.
Saw:         Attached one photo, then clicked Browse again and attached a
             second. Only the second was staged. Nothing warned, and the
             native control shows only "1 file" / "2 files" with no names.
             Verified the other way the same day: two files chosen TOGETHER
             in one dialog both staged correctly ("Your 2 photos are saved"
             on returning via Edit), so the defect is specific to
             sequential selection.
Expected:    A second selection ADDS to the first, and the tenant can see
             exactly which files are attached before sending.
Status:      open — snagged 09 Aug. Folded into
             docs/attachment-policy-design.md §4/§5.
Notes:       Not a bug in our code — an HTML file input replaces its entire
             FileList on each selection. That makes it invisible server-side:
             CaseController::store receives one file and validates it
             happily. No test could catch it; it lives entirely in the
             browser.
             Compounded by #39 — the preview shows no attachments at all, so
             the tenant's first sight of the truth is the case page, after
             the letter has gone to the landlord.
Fix:         Accumulate selections client-side in a DataTransfer and render
             a list (filename, size, remove control, running count) on the
             create form. Enhancement only — the server stays authoritative.
             See the design note; the app already runs JS
             (layouts/app.blade.php:17-27, :310, @yield('scripts') at :311).
--------------------------------------------------------------------
#44
Page:        Create-case form — the "your photo is saved" cue persists from
             an ABANDONED case into a new one
             (CaseController.php:312-321, cases/create.blade.php:109-116).
             Surfaced 2026-08-09 on prod.
Severity:    functional — actively misleads the tenant into NOT attaching
             evidence. Worse than the stale-message it looks like.
Saw:         After abandoning a case at the preview step, starting a new
             case still showed "Your photo is saved — you don't need to
             re-attach it unless you want to change your selection."
Expected:    A fresh case creation starts with no staged photos and no cue.
Status:      open — snagged 09 Aug. Folded into
             docs/attachment-policy-design.md §5.
Notes:       Cause: preview.blade.php:34 links Edit to a bare
             route('cases.create'), so create() cannot distinguish
             "returning via Edit" (where the cue is correct and wanted)
             from "starting fresh" (where it is a lie). The preview payload
             is ONE session key per user, not per case, and is never
             cleared when a creation is abandoned.
             The harm chain: tenant reads the cue on a NEW case, does not
             attach, store() then overwrites the payload with an empty
             photo array, and letter 1 goes out bare. #39 means the preview
             does not catch it either. Every checkpoint that could correct
             the belief is missing.
             CORRECTION 09 Aug: an earlier draft of this entry said
             abandoned payloads leave orphaned files that "nothing
             sweeps". That is WRONG and is retracted.
             SilenceSweep::cleanupPreviewPhotos (SilenceSweep.php:852-869)
             deletes cases/preview/{user}/{random}/ folders older than 24h
             on live runs. The FILES were always cleaned; it is the
             SESSION KEY that outlives them, and the session key is what
             drives the misleading cue. See #45 for what the 24h deletion
             does to a draft confirmed the next day.
Fix:         Give Edit an explicit resume marker
             (route('cases.create', ['resume' => 1])); treat a staged
             payload as resumable only with that marker; otherwise clear it
             and delete its staged files. Add a test for
             abandon-then-start-new asserting no cue and no staged count.
--------------------------------------------------------------------
#45
Page:        CaseController::promotePreviewPhotos (:567-579) — an
             attachment row is written for a staged file that no longer
             exists. Found 2026-08-09 reading the path for the attachment
             policy build.
Severity:    functional — writes evidence into the record that is not
             there, and breaks the send. Narrow trigger, bad outcome.
Saw:         The method guards the file MOVE with
             if ($disk->exists($photo['path'])) but pushes the row into
             $promoted regardless. SilenceSweep::cleanupPreviewPhotos
             (SilenceSweep.php:852-869) deletes preview folders older than
             24h, while the SESSION payload naming those files outlives
             them. So a draft staged one day and confirmed the next writes
             MessageAttachment rows pointing at deleted paths.
Expected:    An attachment that cannot be found is not recorded as sent.
Status:      FIXED 09 Aug on feature/attachment-policy — the entry is
             skipped and a warning logged. Test:
             CaseCreateTest "#45 — drops an attachment whose staged file
             has been swept".
Notes:       Two consequences, both bad. CaseNotice::attachments() calls
             Attachment::fromStorageDisk on the missing path INSIDE a
             queued job, so the letter fails to send from the worker
             rather than anywhere the tenant can see. And the case page
             lists an attachment the tenant does not have — the record
             claims evidence was sent to the landlord when nothing was.
             Same family as #43 and #44: the tenant's belief about what is
             attached diverges silently from what exists.
Fix:         Applied — skip the entry when the source file is gone, and
             Log::warning it. A letter quietly losing its attachment must
             not pass unrecorded.
--------------------------------------------------------------------
#46
Page:        CaseController::store — the Edit round-trip SILENTLY DROPPED
             the staged photos, while the form told the tenant they were
             safe. Found 2026-08-09 in the local browser walk (Charlie:
             "when i do preview and choose edit, the attachments are no
             longer listed and I'm unsure which I uploaded").
Severity:    functional — SILENT EVIDENCE LOSS on the most ordinary path
             through the form. Pre-existing; NOT introduced by the
             attachment work, only found by it.
Saw:         store() unconditionally re-staged whatever the request
             carried: stagePreviewPhotos($request->file('photos', []))
             then session()->put(... 'photos' => $previewPhotos). A
             browser CANNOT re-seed a file input (security), so the second
             POST of an Edit round-trip legitimately carries no files —
             and the payload's photos were overwritten with [].
             So: attach a photo, preview, spot a typo, click Edit, fix one
             word, resubmit, send. The letter goes WITHOUT the photo.
             Meanwhile the form said "Your photo is saved — you don't need
             to re-attach it unless you want to change your selection."
             That cue was FALSE for as long as it has existed.
Expected:    A staged photo survives an Edit round-trip, and the form
             shows WHICH files are attached rather than counting them.
Status:      FIXED 09 Aug on feature/attachment-policy. Tests:
             CaseCreateTest "#46 — Edit, change a word, resubmit: the
             staged photos survive", plus the explicit-Remove and
             replace-not-append cases.
Notes:       Two failures compounding, which is why it went unnoticed:
             the data loss itself, and a cue that actively asserted the
             opposite. The tenant had no way to detect either — the
             preview showed no attachments at all (#39) until today, so
             the first sight of the truth was the case page after sending.
             The old assertion assertSee('photo is saved') passed
             throughout: it pinned the CUE, not the behaviour the cue
             claimed. A test can be green and still be testing the lie.
Fix:         Applied — resolveStagedPhotos() replaces the unconditional
             re-stage with three explicit cases: new files REPLACE the
             staged set; no files + keep flag CARRIES IT FORWARD; explicit
             Remove CLEARS it. The keep flag defaults to ON whenever a
             staged set exists and is only turned off by a deliberate act,
             so the safe outcome (evidence survives) is what happens with
             no JavaScript at all. An ABSENT flag means keep, never drop.
             The form now lists staged files by name and size instead of
             the count cue, and the test asserts the FILENAME — a stronger
             claim than the cue it replaced.
--------------------------------------------------------------------
#47
Page:        Case status handling is OPT-OUT everywhere — a new
             CaseStatus value defaults to "sweep it and run a clock on
             it". Found 2026-08-09 while ruling on #25 (Charlie: "are
             you sure the ladder can happily cope with non-meaning
             statuses").
Severity:    minor TODAY (no unclassified status exists), but the
             failure mode is FUNCTIONAL and silent, and it is armed the
             moment anyone adds an enum value. Blocks #25.
Saw:         Two independent deny-lists, both opt-out:
               - SilenceSweep.php:104 — whereNotIn('status', [Resolved,
                 Abandoned, Dormant, EscalationExhausted]). Anything
                 not named here IS SWEPT.
               - SilenceClock.php:60 — NO_CLOCK_STATUSES, same four.
                 Anything not named here GETS A SILENCE CLOCK.
             There are likely more (display predicates, policy); the
             two above are what a first look found.
Expected:    Adding a status forces an explicit decision about it,
             rather than defaulting to the most dangerous behaviour.
Status:      open — snagged 09 Aug. PREREQUISITE for #25: that build
             adds a terminal `contact_failed` status, and if either
             list were missed the sweep would keep escalating cases we
             had just declared unreachable — reintroducing the exact
             defect #25 exists to fix, via its own fix.
Notes:       The direction of the default is the whole problem. A
             forgotten entry does not produce an inert case, it
             produces an ACTIVELY ESCALATING one. On a product whose
             output is a legal-ish record, the safe default has to be
             "do nothing until told".
             This also makes statuses genuinely expensive, which is
             what drove the #25 ruling (R8) to keep delivery outcomes
             on case_messages rather than in the status enum: no
             predicate branches on a message column, so those carry no
             equivalent risk.
Fix:         A test that enumerates CaseStatus::cases() and asserts
             every value is explicitly classified in each list — swept
             or not-swept, clock or no-clock. Adding an enum value then
             FAILS THE SUITE until someone decides what it means,
             instead of silently defaulting to escalate. ~10 lines.
             It will also flush out the opt-out lists not yet found,
             since each needs the same treatment.
             Consider inverting the lists to allow-lists afterwards,
             but the test alone removes the hazard and is far cheaper.
--------------------------------------------------------------------
#49
Page:        CaseController::resolveLandlordContact / preview / send —
             the landlord name typed on a case is SILENTLY DISCARDED
             when a landlord_contacts row already exists for that
             email, AND the preview renders a different name from the
             letter that is actually sent. Found 2026-08-22 in the
             gafol walk of Release 1 (Charlie: "Dear C Watts / It
             should say Dear Larry Landlord as per case entry").
Severity:    FUNCTIONAL, and the second half is evidential. The tenant
             approves one letter at the preview and a DIFFERENT letter
             leaves the building — REPRODUCED ON DEMAND, twice, on
             gafol. Pre-existing; NOT introduced by the attachment
             work, only found by walking it.
Saw:         Case VMRXZP on gafol. "Larry Landlord" was entered on the
             create form; the sent letter opened "Dear C Watts" and the
             admin case view showed Recipient: C Watts. That address
             already carried a landlord_contacts row named "C Watts"
             from earlier testing.
             Two separate faults, compounding:
               (a) CaseController.php:562 — resolveLandlordContact()
                   does LandlordContact::where('email',$email)->first()
                   and returns the existing row VERBATIM. The typed
                   landlord_name is thrown away with no message. The
                   table is keyed GLOBALLY on email, so first write
                   wins forever, across every tenant: whoever names an
                   address first names it for everyone.
               (b) The two surfaces read DIFFERENT SOURCES.
                   CaseController.php:470 (preview) renders
                   $validated['landlord_name'] — what the tenant just
                   typed. SendCaseNotice.php:302 (send) renders
                   $case->landlordContact->name — the stored row. When
                   they disagree, the preview shows a letter that was
                   never sent.
             (b) CONFIRMED IN THE BROWSER 22 Aug by a repeat run on
             gafol: the preview showed the CORRECT typed landlord name,
             and the resulting email did not. Observed, not inferred.
Expected:    The name a tenant types is the name their landlord reads,
             or they are TOLD it will not be. Whatever the preview
             shows is the letter that goes.
Status:      open — snagged 22 Aug during the Release 1 gafol walk.
             Testing unblocked by UPDATEing the row, or by using a
             fresh unused email per test case.
Notes:       Live evidence for the D0 candidate in
             docs/landlord-contact-model-gap.md — global email-keyed
             contacts, no per-property ownership, no change history.
             That doc is sequenced after #25 at ~5-6 days; this is the
             first time the gap has been seen to CORRUPT A SENT LETTER
             rather than merely being untidy.
             Same family as #46: a surface asserted something the
             behaviour did not honour. #46 was the form's "photo is
             saved" cue; this is the preview itself. The frozen
             body_raw invariant HOLDS — what was sent is what is
             recorded — but the tenant's approval was given against
             different text, so the freeze is preserving a letter
             nobody agreed to.
             Note the two fixes have very different costs and can be
             taken separately.
Fix:         (b) FIRST — it is cheap and it is what breaks trust in
             the preview. Make preview resolve the contact the same
             way send does, so both render one source. Test: create a
             case against an email whose stored name DIFFERS from the
             typed one, assert the preview and the frozen body_raw
             carry the SAME salutation. That test fails today.
             (a) is the model change and belongs with the
             landlord-contact-model D0, not before it. Interim
             mitigation if that is far off: when an existing row is
             returned and the typed name differs, say so on the
             preview rather than discarding it in silence.
--------------------------------------------------------------------
#50
Page:        Severity is collected as a REQUIRED field and then reaches
             nobody. It is absent from the preview, from the letter,
             and from the D9 header block. Found 2026-08-22 in the
             gafol walk of Release 1 (Charlie: "Severity setting does
             not appear on preview or letter").
Severity:    functional/content. Not silent data loss — the value is
             stored correctly — but the tenant is made to classify the
             urgency of their own repair and that classification never
             leaves the building.
Saw:         cases.severity is an enum routine|serious|emergency
             (migration 2026_05_03_160918, default routine), required
             at CaseController.php:417, cast on the model, and shown
             to the TENANT on the case page (show.blade.php:54).
             It is not in LetterTemplateRenderer::WHITELIST, so even
             if someone wrote {{severity}} into a template body it
             would render as the LITERAL text "{{severity}}" rather
             than substituting — the allow-list is the gate.
             It is not in the preview vars (CaseController.php:468) and
             not passed by SendCaseNotice (:295-310).
             Nor does it drive any behaviour: response_days comes from
             the global escalation.interval_days setting, so an
             emergency and a routine case get the SAME 14 days and a
             textually IDENTICAL letter. Grepping the app, severity
             appears only in the form, the store, the model/factory/
             migration, the dev:* commands, and the tenant case page.
Expected:    Either the landlord is told how urgent the tenant says it
             is, or the tenant is not asked to say.
Status:      open — snagged 22 Aug during the Release 1 gafol walk.
             Pre-existing; nothing to do with attachments.
Notes:       This is a DESIGN question before it is a code change, and
             it belongs to the design doc, not a brief. Adding
             {{severity}} to the whitelist is ten minutes; deciding
             what an "emergency" claim DOES is not.
             Two things to settle first:
               - Does severity change the response deadline? An
                 emergency with the same 14 days reads oddly, but a
                 tenant-chosen field that shortens a legal-ish
                 deadline is a much bigger claim and interacts with
                 D4 interval snapshotting.
               - Is a tenant's self-assessment safe to put in an
                 evidential letter at all? "EMERGENCY" asserted by the
                 claimant, in a document meant to be neutral, is a
                 weaker artefact than the facts stated plainly. The
                 issue description already carries the substance.
             A defensible outcome is to KEEP severity as internal
             triage, drop it from the form's prominence, and say so —
             rather than surfacing it because it happens to exist.
Fix:         Decide the above first. If it is to be shown: add
             'severity' to LetterTemplateRenderer::WHITELIST, pass it
             from BOTH the preview vars and SendCaseNotice (see #49 —
             those two must read one source), and put it in the D9
             header block so every outbound letter carries it. If it
             is to stay internal: record that ruling in the design doc
             and leave the letter alone.
--------------------------------------------------------------------
#51
Page:        Postcode is format-checked but never verified to EXIST, and
             nothing cross-checks it against the city the tenant typed.
             Raised 2026-08-22 during the Release 1 gafol walk (Charlie:
             "need to have postcode validation - the free service that
             just provides city").
Severity:    functional, and EVIDENTIAL at the sharp end — the property
             address is the thing the letter identifies. A notice that
             names an address which does not exist is a weaker
             artefact, and the tenant has no way to know.
Saw:         PropertyController.php:26 POSTCODE_PATTERN is a shape
             regex only: /^[A-Z]{1,2}\d[A-Z\d]?\s*\d[A-Z]{2}$/i, with
             normalisePostcode() upper-casing and re-spacing. Any
             well-formed string passes, whether or not Royal Mail has
             ever issued it. properties.city is a separate required
             free-text column (100 chars) with no relationship to it.
             Live example from the walk: "23 New Street, Henley,
             RG1 5RD" was accepted. RG1 is READING, not Henley. The
             letter's address block (SendCaseNotice::propertyAddress)
             joins line1, line2, city, postcode — so the wrong town
             went out to the landlord inside the notice.
Expected:    A postcode that does not exist is refused. A city that
             disagrees with the postcode is questioned before the
             letter carries it.
Status:      open — snagged 22 Aug. Feature, not a regression.
Notes:       postcodes.io is the obvious free service: UK-wide, no API
             key, no registration, open data, and it returns the
             admin_district that gives the town. Caveats to weigh
             BEFORE building on it:
               - It is a third party in the request path. It must
                 degrade OPEN: if the lookup is down or slow, the
                 tenant still registers their property. A repair case
                 must never be blocked by an address-tidying service.
               - It sends a tenant's home postcode to an outside host.
                 Small, but this project has already declined that
                 trade once on principle — the 1 Aug DMARC change
                 dropped ruf/fo precisely because forensic reports
                 forward tenant data to third parties. Same question,
                 same answer needed, not assumed.
               - admin_district is the local authority, NOT the Royal
                 Mail post town. They differ often enough that a hard
                 equality check would reject correct addresses.
             So the useful shape is SUGGEST, not ENFORCE: verify the
             postcode exists (hard rule, purely local consequence),
             and where the district disagrees with the typed city,
             offer the lookup's answer rather than overruling. The
             tenant lives there; they are the authority on their own
             address.
Fix:         Two independent steps, smallest first.
             (1) Existence check against postcodes.io on the property
                 create/edit path, with a short timeout and a
                 fail-open fallback to today's regex. Cache results —
                 postcodes do not move.
             (2) City reconciliation as a soft prompt on the same
                 form: "RG1 5RD looks like Reading — is that right?"
                 with the tenant's own value kept if they say yes.
             Test both with the service stubbed, including the
             fail-open path, so the suite never depends on a live
             third-party host.
--------------------------------------------------------------------
#52
Page:        The admin case view renders a DIFFERENT, shorter property
             address than the letter does. Found 2026-08-22 while
             investigating #49 (not separately reported).
Severity:    minor — cosmetic today, but it is the drift pattern that
             #49 is the serious version of.
Saw:         admin/cases/show.blade.php:66 hardcodes
             "{{ $case->property?->address_line1 }},
              {{ $case->property?->postcode }}"
             — dropping address_line2 AND city. The letter uses
             SendCaseNotice::propertyAddress(), which joins all four.
             So case VMRXZP read "23 New Street, RG1 5RD" on the admin
             oversight screen while the letter carried the city too.
             The admin screen is where you check what a case says; it
             should not quietly show less than what was sent.
Expected:    One address formatter, used everywhere.
Status:      open — snagged 22 Aug.
Notes:       Third private copy of the same logic. There is already a
             formatAddress() in CaseController (:843) and
             propertyAddress() in SendCaseNotice, SendExhaustionCloser,
             HandleInboundReply and SilenceSweep. The FileSize class
             docblock makes exactly this argument for sizes — "a
             private format in each view is how they drift" — and the
             address has more copies than the size ever had.
Fix:         Pull the address format onto the Property model (an
             accessor, e.g. $property->formatted_address) or into a
             small support class beside FileSize, and point all call
             sites at it, admin view included. Mechanical.
--------------------------------------------------------------------
#53
Page:        Edit-from-preview: clicking Remove on ONE of two staged
             photos deletes BOTH. Found 2026-08-22 in the gafol walk of
             Release 1 (Charlie: "doing edit from preview and deleting
             1 of 2 attachments actually deletes both files").
Severity:    functional — evidence loss, though VISIBLE rather than
             silent (see Notes). **NOT pre-existing. This ships WITH
             Release 1** — the per-file Remove control is new in the
             attachment work, so this is a defect in the #46 fix, not
             something it uncovered.
Saw:         The staged set is all-or-nothing on BOTH sides, while the
             UI offers per-file control.
               - create.blade.php:172-179 renders a [data-remove-staged]
                 Remove button INSIDE the @foreach, so each staged photo
                 gets its own.
               - create.blade.php:321-326 — the click handler calls
                 dropStaged() for ANY of those buttons, ignoring which
                 row was clicked.
               - dropStaged() (:292-295) removes EVERY [data-staged] row
                 from the DOM and sets keep_staged_photos = '0'.
               - Server side, CaseController::resolveStagedPhotos()
                 (:611) reads that flag as a single boolean for the
                 WHOLE set: staged && !keep -> discardStagedPhotos(),
                 return []. There is no per-file identity in the
                 instruction, so the server COULD NOT honour a partial
                 removal even if the script asked for one.
             So the control says "remove this photo" and the system
             hears "remove all photos".
Expected:    Remove on one staged photo removes that photo. The others
             survive the round-trip.
Status:      open — snagged 22 Aug during the Release 1 gafol walk.
             UNREACHABLE AT THE SHIPPED DEFAULT (see Notes).
Notes:       Two things soften this, and one sharpens it.
             Softening:
               - It is NOT silent. dropStaged() clears the rows from
                 the list immediately, so both photos visibly vanish in
                 front of the tenant. Unlike #46, an attentive tenant
                 sees it happen. They just cannot get one back without
                 re-attaching both.
               - attachments.first_notice_max SHIPS AT 1, and with a
                 ceiling of 1 there is never a second staged photo, so
                 the defect cannot occur on a default install. It is
                 LATENT, and armed the moment an admin raises the
                 ceiling to 2 or 3 — which is the entire point of the
                 setting existing.
             Sharpening:
               - It is the SAME FAMILY as #46 and #49: a surface
                 asserting something the behaviour does not honour.
                 Three now, in one feature area. #46 was the "photo is
                 saved" cue, #49 the preview's salutation, this the
                 per-file Remove button. Worth treating as a pattern
                 rather than three coincidences — the letter path keeps
                 growing controls whose promises nothing enforces.
               - The suite did not catch it because the #46 tests
                 (explicit-Remove, replace-not-append) all run with a
                 SINGLE staged photo, where all-or-nothing and
                 per-file are indistinguishable. Any test at ceiling 2
                 would have failed.
Fix:         The keep flag has to stop being one boolean for the set.
             Replace it with a per-file instruction the server can act
             on — e.g. a hidden keep_staged_photos[] carrying the
             staged paths that survive, with resolveStagedPhotos()
             intersecting the payload against it and discarding only
             the dropped ones. Keep the ABSENT-means-keep default: an
             absent array must still mean "keep everything", so a
             caller that forgets the field cannot wipe evidence.
             Then the per-row handler passes the clicked row's identity
             instead of calling dropStaged() blindly.
             TEST AT CEILING 2 OR 3, not 1 — attach two, remove the
             first, assert the SECOND still reaches the sent letter and
             the first does not. That test fails today and is the whole
             point.
             RELEASE CALL: shipping at ceiling 1 keeps this unreachable
             and is defensible. But the ceiling is the feature, so this
             needs fixing before anyone is told they can raise it —
             and #40, the defect that started the attachment work, was
             a photo problem found in the wild. Do not leave a known
             evidence-loss path behind an admin toggle without saying
             so plainly in the release note.
--------------------------------------------------------------------
#54
Page:        Attachment behaviour at ceiling >= 2 is NOT fully tested,
             and cannot cheaply be. Recorded 2026-08-22 at the end of
             the Release 1 gafol walk (Charlie: "its actually tricky to
             test, many combinations ... more to be done on
             attachments").
Severity:    coverage gap, not a defect. Recorded so the untested area
             is a KNOWN boundary rather than an assumed-good one.
Saw:         The walk proved: ceiling honoured at 1 and at 2; a letter
             delivered with 2 attachments; the admin field present and
             range-checked. It also found #53. It did NOT complete the
             #46 Edit round-trip variants, #44, ceiling 0, the preview
             negative, #43 sequential selection, or the oversize-with-
             survivors case.
             The reason it stalled is structural, not effort: the state
             space multiplies. Ceiling (0-3) x staged/new/removed/
             replaced x first-submit vs Edit round-trip x within/over
             size x one-file vs many. Most of it lives in the BROWSER
             — a file input cannot be re-seeded by script, which is the
             very fact behind #46 and #43 — so the suite cannot reach
             it and every combination is a manual walk.
Expected:    The tested configuration and the shipped configuration are
             the same thing, and the untested part is named.
Status:      open — recorded 22 Aug. Deliberate scope call, not a
             defect found and ignored.
Notes:       The important observation is that the matrix is almost
             ENTIRELY a ceiling >= 2 matrix. At ceiling 1 it collapses:
             one photo means no partial removal (#53 unreachable), no
             sequential accumulation (#43 degenerate), no "one of two"
             anything. The small, mostly-proven state space and the
             shipped default are the SAME configuration — which is
             luck worth converting into a deliberate position.
             So ceiling 1 is not merely the default. It is the tested
             capability. Ceiling 2 and 3 are built, reachable from the
             admin surface, and NOT released in any meaningful sense.
Fix:         Two things, cheap.
             (1) Treat ceiling >= 2 as unreleased: say so in the
                 release note and in the admin field's help text, so
                 raising it is an informed act rather than a setting
                 that looks as finished as the others.
             (2) When the ceiling work resumes, walk it as a written
                 matrix rather than ad hoc, and fix #53 first — it
                 contaminates every multi-photo test until it is gone.
             Neither blocks Release 1 at ceiling 1.
--------------------------------------------------------------------

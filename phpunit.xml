<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            PHP's default is 128M, which was never a decision about this
            suite — it is just the default. The suite passed at 696 tests
            and hit a fatal at 701, so it had been running within about a
            percent of the ceiling for some time and the next few tests
            were always going to be the ones that tipped it, whatever they
            happened to be.

            Measured, not guessed: the full suite passes at 192M, so this
            was marginality rather than a leak. 256M leaves real headroom
            without being so generous that a genuine leak could hide
            behind it. If this ever fails again, measure before raising it
            a second time.
        -->
        <ini name="memory_limit" value="256M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="MAILGUN_CASES_FROM_ADDRESS" value="cases@mg.renters.rent"/>
        <env name="MAILGUN_INBOUND_DOMAIN" value="inbox.renters.rent"/>
        <env name="DEV_TENANT_EMAIL" value="tenant@inbox.test"/>
        <env name="DEV_LANDLORD_EMAIL" value="landlord@inbox.test"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
